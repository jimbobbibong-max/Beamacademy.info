---
phase: 06-hero-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Visitor sees a portal mockup showing score tracking in the hero"
    - "Portal mockup displays topic scores with visual indicators"
    - "Mockup shows 'weak topics' concept (lower scores highlighted)"
    - "Visual works on mobile without horizontal scroll"
  artifacts:
    - path: "index.html"
      provides: "Hero visual with portal mockup replacing classroom image"
      contains: "hero-portal-mockup"
  key_links:
    - from: ".hero-portal-mockup"
      to: "hero-visual"
      via: "replaces classroom image"
      pattern: "hero-visual.*hero-portal-mockup"
    - from: "portal mockup content"
      to: "score tracking concept"
      via: "visible percentages and weak topics"
      pattern: "Weak Topics|score-(green|yellow|red)"
---

<objective>
Replace the hero visual (classroom photo) with a portal mockup that demonstrates score tracking and weak topic identification.

Purpose: The current hero shows a classroom image which doesn't communicate the differentiator. A portal mockup showing scores and weak topics provides visual proof of the tracking system mentioned in the headline. This satisfies HERO-02.

Output: Hero visual section contains a styled portal mockup showing performance data and weak topics instead of the classroom photograph.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hero-overhaul/06-CONTEXT.md
@.planning/phases/06-hero-overhaul/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hero image with portal mockup HTML</name>
  <files>index.html (lines 4510-4521 hero-visual section)</files>
  <action>
    Replace the hero-visual content with a portal mockup that demonstrates score tracking:

    **Current structure to replace (lines 4510-4522):**
    ```html
    <div class="hero-visual">
        <div class="hero-card hero-image-card">
            <img src="images/classroom-teaching.jpg" ...>
        </div>
        <div class="floating-card card-1">...</div>
        <div class="floating-card card-2">...</div>
    </div>
    ```

    **New structure:**
    ```html
    <div class="hero-visual">
        <div class="hero-card hero-portal-mockup">
            <div class="hero-mockup-header">
                <div class="hero-mockup-dots" aria-hidden="true">
                    <span></span>
                    <span></span>
                    <span></span>
                </div>
                <div class="hero-mockup-title">Performance Dashboard</div>
            </div>
            <div class="hero-mockup-content">
                <div class="hero-mockup-section">
                    <div class="hero-mockup-label">Current Score</div>
                    <div class="hero-mockup-score">
                        <span class="score-value">78%</span>
                        <span class="score-badge score-yellow">Needs Practice</span>
                    </div>
                </div>
                <div class="hero-mockup-section">
                    <div class="hero-mockup-label">Topic Breakdown</div>
                    <div class="hero-mockup-row">
                        <span>Algebra</span>
                        <span class="score-green">92%</span>
                    </div>
                    <div class="hero-mockup-row">
                        <span>Trigonometry</span>
                        <span class="score-yellow">71%</span>
                    </div>
                    <div class="hero-mockup-row">
                        <span>Calculus</span>
                        <span class="score-red">64%</span>
                    </div>
                </div>
                <div class="hero-mockup-section hero-mockup-weak">
                    <div class="hero-mockup-label">Weak Topics</div>
                    <div class="hero-mockup-tags">
                        <span class="weak-tag">Calculus - Derivatives</span>
                        <span class="weak-tag">Trigonometry - Identities</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    ```

    **Key design decisions:**
    - Remove floating cards (they add visual noise and don't communicate the value)
    - Show "Current Score" with a status badge (demonstrates tracking)
    - Show "Topic Breakdown" with color-coded percentages (green/yellow/red)
    - Show "Weak Topics" section (this is the differentiator - we identify specific weaknesses)
    - Use dark background like existing portal mockup for contrast

    **Data authenticity (from RESEARCH.md):**
    - Based on real Dashboard.jsx: scores, topic breakdown, weak topics
    - Numbers should look realistic (mix of good and bad scores)
    - Include at least one topic in "red" to show weakness identification
  </action>
  <verify>
    - Open index.html in browser
    - Verify portal mockup appears in hero visual area
    - Verify mockup shows: Current Score, Topic Breakdown, Weak Topics
    - Verify classroom image is gone
    - Verify floating cards are removed
  </verify>
  <done>
    Hero visual now shows portal mockup with score tracking and weak topics
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for hero portal mockup</name>
  <files>index.html (CSS section, around lines 770-790)</files>
  <action>
    Add CSS for the hero portal mockup. Insert after the `.hero-image-card` styles (around line 786):

    ```css
    /* Hero Portal Mockup */
    .hero-portal-mockup {
        background: var(--gray-900);
        padding: 0;
        overflow: hidden;
    }

    .hero-mockup-header {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 16px 24px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .hero-mockup-dots {
        display: flex;
        gap: 8px;
    }

    .hero-mockup-dots span {
        width: 12px;
        height: 12px;
        border-radius: 50%;
    }

    .hero-mockup-dots span:nth-child(1) { background: #ff5f56; }
    .hero-mockup-dots span:nth-child(2) { background: #ffbd2e; }
    .hero-mockup-dots span:nth-child(3) { background: #27ca40; }

    .hero-mockup-title {
        color: var(--gray-400);
        font-family: var(--font-display);
        font-size: 0.9rem;
        font-weight: 500;
    }

    .hero-mockup-content {
        padding: 24px;
    }

    .hero-mockup-section {
        margin-bottom: 24px;
    }

    .hero-mockup-section:last-child {
        margin-bottom: 0;
    }

    .hero-mockup-label {
        color: var(--gray-500);
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 12px;
    }

    .hero-mockup-score {
        display: flex;
        align-items: center;
        gap: 16px;
    }

    .score-value {
        font-family: var(--font-display);
        font-size: 2.5rem;
        font-weight: 700;
        color: white;
    }

    .score-badge {
        padding: 6px 12px;
        border-radius: 100px;
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
    }

    .score-badge.score-green { background: rgba(39, 202, 64, 0.2); color: #27ca40; }
    .score-badge.score-yellow { background: rgba(255, 189, 46, 0.2); color: #ffbd2e; }
    .score-badge.score-red { background: rgba(255, 95, 86, 0.2); color: #ff5f56; }

    .hero-mockup-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        color: var(--gray-300);
        font-size: 0.95rem;
    }

    .hero-mockup-row:last-child {
        border-bottom: none;
    }

    /* Score colors (reuse from portal-mockup if not already global) */
    .score-green { color: #27ca40; font-weight: 600; }
    .score-yellow { color: #ffbd2e; font-weight: 600; }
    .score-red { color: #ff5f56; font-weight: 600; }

    .hero-mockup-weak {
        background: rgba(255, 95, 86, 0.1);
        padding: 16px;
        border-radius: 12px;
        margin-top: 16px;
    }

    .hero-mockup-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
    }

    .weak-tag {
        background: rgba(255, 95, 86, 0.2);
        color: #ff5f56;
        padding: 6px 12px;
        border-radius: 6px;
        font-size: 0.8rem;
        font-weight: 500;
    }
    ```

    **Note:** Some score-color classes may already exist from the portal section (lines 1812-1813). Check if `.score-green`, `.score-yellow` exist and reuse them, or add `.score-red` if missing.
  </action>
  <verify>
    - Open browser dev tools
    - Verify mockup has dark background (gray-900)
    - Verify macOS-style dots appear (red, yellow, green)
    - Verify scores have color coding
    - Verify weak topics section has red tint
  </verify>
  <done>
    Portal mockup is styled consistently with existing portal section but adapted for hero prominence
  </done>
</task>

<task type="auto">
  <name>Task 3: Add mobile responsive CSS for mockup</name>
  <files>index.html (mobile CSS section, around lines 3174-3221)</files>
  <action>
    Add mobile-specific styles for the hero portal mockup. Insert within the `@media (max-width: 768px)` block:

    ```css
    /* Hero portal mockup mobile adjustments */
    .hero-portal-mockup {
        margin-top: 32px;
    }

    .hero-mockup-header {
        padding: 12px 16px;
    }

    .hero-mockup-content {
        padding: 16px;
    }

    .score-value {
        font-size: 2rem;
    }

    .hero-mockup-row {
        font-size: 0.85rem;
        padding: 8px 0;
    }

    .hero-mockup-tags {
        flex-direction: column;
    }

    .weak-tag {
        font-size: 0.75rem;
    }
    ```

    **Also check/update the tablet breakpoint** (around line 2848) to ensure mockup works at 1024px:
    - The grid already collapses to single column at 1024px
    - Mockup should stack below text content naturally
    - No additional changes needed if grid handles it

    **Remove floating card hiding** if still present:
    - The `.floating-card { display: none; }` rule at line 3210 can stay as a safety
    - But since we removed floating cards from HTML, this is now a no-op
  </action>
  <verify>
    Test at mobile width (< 768px):
    - Mockup appears below hero text content
    - No horizontal scroll
    - Score value is readable but smaller
    - Weak topic tags stack vertically
    - Overall mockup fits within viewport width

    Test at tablet width (768-1024px):
    - Grid collapses to single column
    - Mockup appears centered below text
  </verify>
  <done>
    Portal mockup is fully responsive and works on all screen sizes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hero visual replaced with portal mockup showing score tracking and weak topics</what-built>
  <how-to-verify>
    1. Open http://localhost:PORT or file:///path/to/index.html in browser
    2. View at desktop width (1200px+):
       - Verify portal mockup appears on right side of hero
       - Verify it shows: Current Score (78%), Topic Breakdown, Weak Topics
       - Verify color coding: green (92%), yellow (71%), red (64%)
       - Verify "Weak Topics" section is visible with red tags
    3. View at mobile width (375px):
       - Verify mockup stacks below hero text
       - Verify all content is readable
       - Verify no horizontal scroll
    4. Visual quality check:
       - Does mockup look professional?
       - Does it communicate "we track scores and find weak spots"?
       - Does it match the existing site design (colors, typography)?
  </how-to-verify>
  <resume-signal>Type "approved" if mockup looks good, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. **HERO-02 (Portal screenshot):** Is a portal mockup visible in the hero?
   - Verify mockup exists in hero-visual div
   - Verify it shows score tracking data
   - Verify it shows weak topics identification

2. **Visual authenticity:**
   - Does mockup look like a real dashboard?
   - Are the scores realistic (not all perfect)?
   - Does it clearly show weak topics?

3. **Mobile compatibility:**
   - Test at 320px, 375px, 428px widths
   - No horizontal scroll
   - All content readable

4. **No regressions:**
   - Page still loads quickly (no large images added)
   - Hero section still appears above the fold
   - Other sections unaffected
</verification>

<success_criteria>
- [ ] Portal mockup visible in hero (replacing classroom image)
- [ ] Mockup shows Current Score with status badge
- [ ] Mockup shows Topic Breakdown with color-coded scores
- [ ] Mockup shows Weak Topics section
- [ ] Works on mobile (320px-428px) without horizontal scroll
- [ ] Visual approved by user (checkpoint passed)
</success_criteria>

<output>
After completion, create `.planning/phases/06-hero-overhaul/06-02-SUMMARY.md`
</output>
