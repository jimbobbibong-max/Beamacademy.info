---
phase: 06-hero-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Visitor sees 'HSC Tutoring' category label without scrolling"
    - "Headline communicates custom materials/personalization value"
    - "Subheadline explains the tracking mechanism in plain English"
    - "Only one primary CTA button exists in hero"
  artifacts:
    - path: "index.html"
      provides: "Hero text content with category, headline, subheadline, single CTA"
      contains: "hero-category"
  key_links:
    - from: ".hero-category"
      to: "visible position in hero"
      via: "CSS styling above h1"
      pattern: "hero-category.*HSC Tutoring"
    - from: ".btn-primary"
      to: "#contact"
      via: "single CTA link"
      pattern: "hero-buttons.*btn-primary.*#contact"
---

<objective>
Overhaul the hero text content to immediately communicate what BEAM is (HSC tutoring) and why it's different (custom materials based on score tracking).

Purpose: Visitors currently see "Expert Tutoring for Years 7-12" which is generic and doesn't convey the differentiator. This plan replaces all text elements to satisfy HERO-01, HERO-03, HERO-04, and HERO-05.

Output: Updated hero section with category label, outcome-focused headline, plain English subheadline, and single CTA.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hero-overhaul/06-CONTEXT.md
@.planning/phases/06-hero-overhaul/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hero text content (badge, headline, description, CTA)</name>
  <files>index.html (lines 4489-4525 hero HTML)</files>
  <action>
    Modify the hero section HTML to replace existing text elements:

    1. **Category label (HERO-03):** Replace the badge element:
       - Current: `<div class="hero-badge">Epping, Sydney</div>`
       - New: `<div class="hero-category">HSC Tutoring</div>`
       - Position above the h1 heading

    2. **Headline (HERO-01):** Replace the h1 content:
       - Current: `Expert Tutoring for <span>Years 7-12</span>`
       - New: Parent-focused outcome headline (medium length 7-10 words)
       - Must communicate differentiator (custom materials/where they're struggling)
       - Example direction: "We Show You Exactly Where They're Struggling" or similar
       - Keep the `<span>` for purple accent on key phrase
       - Per CONTEXT.md: Claude has discretion on exact wording within parent-outcome parameters

    3. **Subheadline (HERO-05):** Replace the description paragraph:
       - Current: "Small classes by BHHS graduates with 95%+ in HSC Mathematics. Personalised learning in Epping, Sydney."
       - New: Plain English explanation of the tracking mechanism
       - Must explain HOW the custom materials work (portal tracks scores, identifies weak topics)
       - Avoid jargon like "data-driven" - use plain language like "we track scores"
       - 2-3 sentences max

    4. **Single CTA (HERO-04):** Simplify the buttons div:
       - Current: Primary "Start Free Trial" + Secondary "View Courses"
       - New: Only primary CTA "Book Free Trial" (pointing to #contact)
       - Remove the secondary btn-secondary button entirely
       - Keep the arrow if desired: "Book Free Trial" or "Book Free Trial ->"

    5. **Keep existing elements:**
       - Keep `.hero-returning` paragraph (payment link)
       - Keep `.hero-testimonial` div (social proof)
  </action>
  <verify>
    - Open index.html in browser
    - Verify "HSC Tutoring" label appears above headline
    - Verify headline mentions personalization/custom materials concept
    - Verify only ONE button in hero-buttons div
    - Verify testimonial and returning link still present
  </verify>
  <done>
    Hero text communicates category (HSC Tutoring) and differentiator (custom materials/tracking) with single CTA
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for category label styling</name>
  <files>index.html (lines 672-693 hero-badge CSS)</files>
  <action>
    Add CSS for the new `.hero-category` class in the hero CSS section (around line 670-695):

    1. **Add new class** (insert after .hero-badge styles or replace them):
       ```css
       .hero-category {
           font-family: var(--font-display);
           font-size: 0.85rem;
           font-weight: 600;
           color: var(--purple-600);
           text-transform: uppercase;
           letter-spacing: 0.1em;
           margin-bottom: 16px;
       }
       ```
       This follows the existing `.section-tag` pattern used elsewhere on the site.

    2. **Option to style as badge (alternative):**
       If you want it more prominent like current badge:
       ```css
       .hero-category {
           display: inline-block;
           background: var(--purple-100);
           color: var(--purple-700);
           padding: 8px 16px;
           border-radius: 100px;
           font-family: var(--font-display);
           font-size: 0.85rem;
           font-weight: 600;
           text-transform: uppercase;
           letter-spacing: 0.05em;
           margin-bottom: 20px;
           border: 1px solid var(--purple-200);
       }
       ```
       Choose whichever treatment works better visually.

    3. **Optionally remove or comment out** the .hero-badge:hover and .hero-badge::before styles if they're no longer needed (since we're using hero-category instead).

    4. **Mobile consideration:** The category label should remain visible on mobile. No special mobile override needed as it will naturally flow.
  </action>
  <verify>
    - Open index.html in browser at desktop width
    - Verify category label is styled with purple color, uppercase, proper spacing
    - Open at mobile width (< 768px)
    - Verify category label is visible and properly sized
    - Run: `grep -n "hero-category" index.html` to confirm CSS exists
  </verify>
  <done>
    Category label has proper styling consistent with site design system
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean up removed elements and verify mobile</name>
  <files>index.html</files>
  <action>
    1. **Verify secondary CTA removal:** Ensure only one button remains in `.hero-buttons`:
       - Search for "btn-secondary" in hero section - should not exist
       - The only button should be `btn-primary`

    2. **Verify mobile layout:**
       - Check that mobile CSS (lines 3174-3190) still works with single button
       - The `.hero-buttons .btn { width: 100%; }` rule will make single CTA full-width on mobile
       - No changes needed if button structure is correct

    3. **Optional: Remove Epping from badge area:**
       - Epping, Sydney is now removed from hero
       - Verify it still exists in footer and navigation for local SEO
       - This is informational only - no action needed if Epping is in footer

    4. **Test responsive behavior:**
       - Desktop: Category label above headline, single CTA
       - Tablet (< 1024px): Content stacks, single CTA centered
       - Mobile (< 768px): CTA full width, testimonial visible
  </action>
  <verify>
    Test at three breakpoints:
    - Desktop (1200px+): Layout looks balanced with single CTA
    - Tablet (768-1024px): Content stacks properly
    - Mobile (< 768px): Single CTA is full width, all text readable

    Browser dev tools responsive mode is sufficient.
  </verify>
  <done>
    Hero text overhaul is complete, mobile-responsive, and ready for visual to be updated in Plan 02
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **HERO-01 (Headline):** Does headline communicate category + differentiator?
   - Category: Clear from "HSC Tutoring" label
   - Differentiator: Headline mentions custom materials or score tracking concept

2. **HERO-03 (Category label):** Is "HSC Tutoring" visible without scrolling?
   - Open browser at 1024px width
   - Verify label appears above the fold

3. **HERO-04 (Single CTA):** Is there only one button in the hero?
   - Count buttons in hero-buttons div (should be 1)

4. **HERO-05 (Subheadline):** Is the hook explained in plain English?
   - Read subheadline aloud - does it avoid jargon?
   - Does it explain the tracking/custom materials mechanism?

5. **No regressions:**
   - Testimonial still visible
   - Payment link still functional
   - Mobile layout still works
</verification>

<success_criteria>
- [ ] "HSC Tutoring" category label visible above headline
- [ ] Headline communicates personalization/custom materials value
- [ ] Subheadline explains score tracking in plain English (no jargon)
- [ ] Only ONE primary CTA button in hero
- [ ] Mobile layout works at 320px-428px widths
- [ ] Existing elements (testimonial, payment link) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/06-hero-overhaul/06-01-SUMMARY.md`
</output>
