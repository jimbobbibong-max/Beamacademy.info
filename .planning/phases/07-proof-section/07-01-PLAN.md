---
phase: 07-proof-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: false

must_haves:
  truths:
    - "Parent sees 4 distinct document types at a glance (not scrolling)"
    - "Parent understands documents are free to keep (prominent messaging)"
    - "Parent sees TOPIC-level weakness insight (not just overall score)"
    - "Section visually shows what each document looks like (not just text labels)"
  artifacts:
    - path: "index.html"
      provides: "Proof section with document grid and CSS"
      contains: "proof-section"
  key_links:
    - from: "proof-section HTML"
      to: "existing CSS variables"
      via: "var(--purple-*), var(--gray-*)"
      pattern: "var\\(--purple|var\\(--gray"
    - from: "proof-section layout"
      to: "existing section pattern"
      via: ".section-header, .section-tag, .section-title"
      pattern: "section-header|section-tag|section-title"
---

<objective>
Create Proof Section showing parents the 4 documents they receive from ONE free trial - Diagnostic Worksheet, Answer Key, Worked Solutions, and Progress Report - with visual mockup previews and "Yours to Keep" messaging.

Purpose: Convert hero interest into confidence by answering "what do I actually get?" with visuals instead of text
Output: New section between hero and about with 2x2 document grid
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-proof-section/07-CONTEXT.md
@.planning/phases/07-proof-section/07-RESEARCH.md
@.planning/phases/06-hero-overhaul/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add proof section HTML structure with document grid</name>
  <files>index.html</files>
  <action>
Insert new proof section after the hero section (after line 4717 `</section>` closing hero, before line 4719 `<!-- About Section -->`).

Structure:
```html
<!-- Proof Section -->
<section class="proof-section" id="proof" aria-labelledby="proof-title">
  <div class="container">
    <header class="section-header">
      <div class="proof-badge">From ONE Free Trial - Yours to Keep</div>
      <h2 class="section-title" id="proof-title">See Exactly Where They're Struggling</h2>
      <p class="section-description">Your diagnostic produces 4 custom documents. Keep them even if you don't enrol.</p>
    </header>

    <div class="documents-grid">
      <!-- Progress Report - Top Left (most visual) -->
      <div class="document-card document-dashboard">
        <div class="document-preview" aria-hidden="true">
          <!-- Dashboard mockup here -->
        </div>
        <div class="document-info">
          <h3 class="document-name">Progress Report</h3>
          <p class="document-desc">Topic-by-topic breakdown showing exactly which areas need work</p>
        </div>
      </div>

      <!-- Diagnostic Worksheet - Top Right -->
      <div class="document-card document-paper">
        <div class="document-preview" aria-hidden="true">
          <!-- Paper mockup here -->
        </div>
        <div class="document-info">
          <h3 class="document-name">Diagnostic Worksheet</h3>
          <p class="document-desc">20 targeted questions covering key syllabus topics</p>
        </div>
      </div>

      <!-- Answer Key - Bottom Left -->
      <div class="document-card document-paper">
        <div class="document-preview" aria-hidden="true">
          <!-- Paper mockup here -->
        </div>
        <div class="document-info">
          <h3 class="document-name">Answer Key</h3>
          <p class="document-desc">Quick reference answers for checking work</p>
        </div>
      </div>

      <!-- Worked Solutions - Bottom Right -->
      <div class="document-card document-paper">
        <div class="document-preview" aria-hidden="true">
          <!-- Paper mockup here -->
        </div>
        <div class="document-info">
          <h3 class="document-name">Worked Solutions</h3>
          <p class="document-desc">Step-by-step explanations for every question</p>
        </div>
      </div>
    </div>

    <!-- Topic Gap Insight Callout -->
    <div class="proof-insight">
      <span class="insight-text">The gap most parents miss: specific topics like "Calculus - Derivatives", not just "needs improvement"</span>
    </div>
  </div>
</section>
```

Key implementation notes:
- Use `aria-hidden="true"` on `.document-preview` containers (decorative mockup content)
- Use existing section pattern (section > container > section-header)
- Progress Report positioned top-left per CONTEXT.md decision
- "Yours to Keep" messaging as prominent badge above headline (PROOF-02)
  </action>
  <verify>
grep -n "proof-section" index.html shows section exists
grep -n "documents-grid" index.html shows grid container exists
grep -n "document-card" index.html shows 4 card elements
  </verify>
  <done>
Proof section HTML scaffold inserted between hero and about sections with 4 document card containers, "Yours to Keep" badge, and topic gap callout
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for proof section layout and document cards</name>
  <files>index.html</files>
  <action>
Add CSS in the existing `<style>` block (after hero styles, around line 920+).

```css
/* Proof Section */
.proof-section {
  padding: 100px 0;
  background: var(--purple-50);
}

.proof-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: var(--purple-100);
  color: var(--purple-700);
  padding: 10px 24px;
  border-radius: 100px;
  font-family: var(--font-display);
  font-size: 0.9rem;
  font-weight: 600;
  margin-bottom: 20px;
  border: 1px solid var(--purple-200);
}

/* Documents Grid - 2x2 on desktop, 1 column on mobile */
.documents-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24px;
  max-width: 900px;
  margin: 0 auto;
}

/* Document Card Base */
.document-card {
  background: white;
  border-radius: 20px;
  overflow: hidden;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
  border: 1px solid var(--gray-100);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.document-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 16px 40px rgba(0, 0, 0, 0.1);
  border-color: var(--purple-200);
}

.document-preview {
  height: 200px;
  position: relative;
  overflow: hidden;
}

.document-info {
  padding: 20px 24px;
}

.document-name {
  font-family: var(--font-display);
  font-size: 1.15rem;
  font-weight: 600;
  color: var(--gray-900);
  margin-bottom: 8px;
}

.document-desc {
  font-size: 0.9rem;
  color: var(--gray-500);
  line-height: 1.5;
  margin: 0;
}

/* Topic Gap Insight Callout */
.proof-insight {
  max-width: 600px;
  margin: 40px auto 0;
  text-align: center;
  padding: 16px 24px;
  background: white;
  border-radius: 12px;
  border: 1px solid var(--purple-200);
}

.insight-text {
  font-size: 0.95rem;
  color: var(--gray-600);
  font-style: italic;
}

/* Mobile Responsive */
@media (max-width: 768px) {
  .proof-section {
    padding: 60px 0;
  }

  .documents-grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }

  .document-preview {
    height: 160px;
  }

  .proof-badge {
    font-size: 0.8rem;
    padding: 8px 16px;
  }

  .proof-insight {
    margin-top: 24px;
    padding: 12px 16px;
  }
}
```

Key implementation notes:
- Use existing CSS variables (--purple-50, --gray-100, etc.) for consistency
- 2x2 grid collapses to 1 column at 768px breakpoint (matches site pattern)
- Hover effects match existing card patterns (.hero-card, .feature-card)
- proof-badge uses pill/badge pattern similar to hero-category
  </action>
  <verify>
grep -n "proof-section {" index.html shows section CSS exists
grep -n "documents-grid {" index.html shows grid CSS exists
grep -n "@media.*768px" index.html shows responsive breakpoint includes proof styles
  </verify>
  <done>
Proof section CSS added with 2x2 responsive grid, card styling, badge styling, and mobile breakpoint
  </done>
</task>

<task type="auto">
  <name>Task 3: Add document mockup content for all 4 cards</name>
  <files>index.html</files>
  <action>
Fill in the `.document-preview` containers with CSS-only mockup content. Two visual styles:

**1. Progress Report (Dashboard style) - adapt hero-portal-mockup pattern:**
```html
<div class="document-preview" aria-hidden="true">
  <div class="report-mockup">
    <div class="report-header">
      <span class="report-dots">
        <span class="dot dot-red"></span>
        <span class="dot dot-yellow"></span>
        <span class="dot dot-green"></span>
      </span>
      Progress Report
    </div>
    <div class="report-body">
      <div class="report-score">
        <div class="report-score-value">78%</div>
        <div class="report-score-label">Overall Score</div>
      </div>
      <div class="report-topics">
        <div class="report-topic">
          <span class="topic-name">Algebra</span>
          <span class="topic-score score-green">92%</span>
        </div>
        <div class="report-topic">
          <span class="topic-name">Trigonometry</span>
          <span class="topic-score score-yellow">71%</span>
        </div>
        <div class="report-topic">
          <span class="topic-name">Calculus</span>
          <span class="topic-score score-red">64%</span>
        </div>
      </div>
      <div class="report-focus">
        <div class="focus-label">Focus Areas</div>
        <div class="focus-tags">
          <span class="focus-tag">Derivatives</span>
          <span class="focus-tag">Identities</span>
        </div>
      </div>
    </div>
  </div>
</div>
```

**2. Paper Document Mockups (Worksheet, Answer Key, Solutions):**

Diagnostic Worksheet:
```html
<div class="document-preview" aria-hidden="true">
  <div class="paper-mockup">
    <div class="paper-header">BEAM Academy</div>
    <div class="paper-title">Year 11 Maths - Diagnostic</div>
    <div class="paper-content">
      <div class="paper-question">Q1. Differentiate y = 3x² + 2x - 5</div>
      <div class="paper-question">Q2. Find sin(π/4) + cos(π/3)</div>
      <div class="paper-question">Q3. Solve: 2x² - 5x + 3 = 0</div>
      <div class="paper-question">Q4. Prove that tan²θ + 1 = sec²θ</div>
    </div>
  </div>
</div>
```

Answer Key:
```html
<div class="document-preview" aria-hidden="true">
  <div class="paper-mockup paper-answers">
    <div class="paper-header">BEAM Academy</div>
    <div class="paper-title">Answer Key</div>
    <div class="paper-content">
      <div class="answer-row"><span class="answer-num">1.</span> 6x + 2</div>
      <div class="answer-row"><span class="answer-num">2.</span> (√2 + 1)/2</div>
      <div class="answer-row"><span class="answer-num">3.</span> x = 1.5, x = 1</div>
      <div class="answer-row"><span class="answer-num">4.</span> See solutions</div>
    </div>
  </div>
</div>
```

Worked Solutions:
```html
<div class="document-preview" aria-hidden="true">
  <div class="paper-mockup paper-solutions">
    <div class="paper-header">BEAM Academy</div>
    <div class="paper-title">Worked Solutions</div>
    <div class="paper-content">
      <div class="solution-q">Q1. Differentiate y = 3x² + 2x - 5</div>
      <div class="solution-step">Step 1: Apply power rule</div>
      <div class="solution-step">Step 2: d/dx(3x²) = 6x</div>
      <div class="solution-step">Step 3: d/dx(2x) = 2</div>
      <div class="solution-answer">Answer: dy/dx = 6x + 2</div>
    </div>
  </div>
</div>
```

**Add CSS for mockup content:**

```css
/* Progress Report Mockup (Dashboard style) */
.report-mockup {
  background: var(--gray-900);
  height: 100%;
  display: flex;
  flex-direction: column;
}

.report-header {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 14px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  color: var(--gray-400);
  font-family: var(--font-display);
  font-size: 0.75rem;
  font-weight: 500;
}

.report-dots {
  display: flex;
  gap: 6px;
}

.report-dots .dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
}

.dot-red { background: #ff5f57; }
.dot-yellow { background: #febc2e; }
.dot-green { background: #28c840; }

.report-body {
  padding: 12px 14px;
  flex: 1;
}

.report-score {
  text-align: center;
  padding-bottom: 10px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  margin-bottom: 10px;
}

.report-score-value {
  font-family: var(--font-display);
  font-size: 2rem;
  font-weight: 700;
  color: white;
  line-height: 1;
}

.report-score-label {
  font-size: 0.7rem;
  color: var(--gray-400);
  margin-top: 4px;
}

.report-topics {
  margin-bottom: 10px;
}

.report-topic {
  display: flex;
  justify-content: space-between;
  padding: 5px 0;
  font-size: 0.75rem;
  color: var(--gray-300);
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.topic-score {
  font-weight: 600;
}

.score-green { color: #28c840; }
.score-yellow { color: #febc2e; }
.score-red { color: #ff5f57; }

.report-focus {
  background: rgba(255, 95, 87, 0.1);
  padding: 8px 10px;
  border-radius: 8px;
}

.focus-label {
  font-size: 0.65rem;
  color: var(--gray-400);
  margin-bottom: 4px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.focus-tags {
  display: flex;
  gap: 6px;
  flex-wrap: wrap;
}

.focus-tag {
  background: rgba(255, 95, 87, 0.2);
  color: #ff8a80;
  padding: 2px 8px;
  border-radius: 4px;
  font-size: 0.65rem;
  font-weight: 500;
}

/* Paper Document Mockups */
.paper-mockup {
  background: white;
  height: 100%;
  padding: 14px 16px;
  font-family: 'Times New Roman', Georgia, serif;
  font-size: 0.7rem;
  color: var(--gray-700);
  position: relative;
  overflow: hidden;
}

.paper-mockup::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 50px;
  background: linear-gradient(to bottom, transparent, white);
  pointer-events: none;
}

.paper-header {
  text-align: center;
  font-weight: bold;
  color: var(--purple-700);
  font-family: var(--font-display);
  font-size: 0.75rem;
  margin-bottom: 4px;
}

.paper-title {
  text-align: center;
  font-size: 0.8rem;
  font-weight: bold;
  margin-bottom: 12px;
  color: var(--gray-800);
}

.paper-question {
  margin-bottom: 10px;
  line-height: 1.4;
  padding-left: 4px;
}

/* Answer Key specific */
.answer-row {
  display: flex;
  gap: 8px;
  margin-bottom: 8px;
  line-height: 1.4;
}

.answer-num {
  font-weight: bold;
  min-width: 20px;
}

/* Worked Solutions specific */
.solution-q {
  font-weight: bold;
  margin-bottom: 8px;
  color: var(--gray-800);
}

.solution-step {
  padding-left: 12px;
  margin-bottom: 4px;
  color: var(--gray-600);
}

.solution-answer {
  font-weight: bold;
  margin-top: 8px;
  padding: 4px 8px;
  background: var(--purple-50);
  border-radius: 4px;
  display: inline-block;
}
```

Key implementation notes:
- Report mockup uses dark theme (var(--gray-900)) to match hero portal mockup
- Paper mockups use serif font for document feel, fade at bottom suggests more content
- All mockup content is aria-hidden (decorative only)
- Color-coded scores (green/yellow/red) match hero mockup pattern
- Focus areas section highlighted in red to show topic gap insight (PROOF-04)
  </action>
  <verify>
grep -n "report-mockup" index.html shows dashboard mockup exists
grep -n "paper-mockup" index.html shows paper mockups exist
grep -n "focus-tag" index.html shows topic gap tags exist
  </verify>
  <done>
All 4 document mockups populated with CSS-only visual content: Progress Report (dashboard style showing 78% with topic breakdown and focus areas), Diagnostic Worksheet (math questions), Answer Key (numbered answers), Worked Solutions (step-by-step)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Proof Section with 4 document mockups, "Yours to Keep" messaging, and topic gap insight</what-built>
  <how-to-verify>
1. Open http://localhost:8080 (or run `python -m http.server 8080` in beamacademy-site folder)
2. Scroll past hero section - Proof Section should appear immediately after
3. Verify:
   - "From ONE Free Trial - Yours to Keep" badge is prominent above headline
   - "See Exactly Where They're Struggling" headline is visible
   - 4 document cards in 2x2 grid:
     - Top-left: Progress Report (dark dashboard with scores)
     - Top-right: Diagnostic Worksheet (white paper with math questions)
     - Bottom-left: Answer Key (numbered answers)
     - Bottom-right: Worked Solutions (step-by-step)
   - Each card has title + description below the mockup
   - Topic gap callout below grid mentions "specific topics like Calculus - Derivatives"
4. Resize browser to mobile width (< 768px):
   - Grid should stack to single column
   - No horizontal scroll
   - Badge still readable
5. Hover over cards (desktop):
   - Should lift slightly with shadow
  </how-to-verify>
  <resume-signal>Type "approved" if section looks good, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -c "proof-section" index.html` returns at least 2 (HTML + CSS)
2. `grep -c "document-card" index.html` returns at least 4 (the 4 cards)
3. `grep -c "report-mockup\|paper-mockup" index.html` returns at least 4 (the mockups)
4. Visual verification passes (checkpoint)
</verification>

<success_criteria>
- PROOF-01: 4 documents from ONE trial visualized (4 distinct mockups in grid)
- PROOF-02: "Yours to keep" messaging prominently displayed (badge above headline)
- PROOF-03: Documents shown with visual previews (CSS-only mockups)
- PROOF-04: Topic gap insight shown (Focus Areas section in Progress Report + callout)
- Section appears immediately after hero (before About section)
- Mobile responsive (1 column at 768px)
- No LCP regression (CSS-only, no images)
</success_criteria>

<output>
After completion, create `.planning/phases/07-proof-section/07-01-SUMMARY.md`
</output>
