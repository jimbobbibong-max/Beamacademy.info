---
phase: 22-layout-mobile-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: true

must_haves:
  truths:
    - "Trust section tutor cards have intentional staggered/offset layout on desktop"
    - "Mobile view shows single-column layout without offsets (clean fallback)"
    - "Editorial visual tension created through vertical offset pattern"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Asymmetric tutor grid CSS"
      contains: "tutor-card:nth-child"
  key_links:
    - from: ".trust-tutors grid"
      to: "tutor-card offsets"
      via: "CSS nth-child selectors"
      pattern: "tutor-card:nth-child.*margin-top"
---

<objective>
Apply intentional asymmetric layout to the Trust section tutor cards grid.

Purpose: Create visual tension and editorial feel per Frontend Design Skill principles, while maintaining readability and professionalism for parent audience.

Output: Trust section with staggered tutor card layout that resets cleanly on mobile.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-layout-mobile-conversion/22-RESEARCH.md

Current trust-tutors CSS (line ~1628):
```css
.trust-tutors {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 24px;
    justify-content: center;
    max-width: 1100px;
    margin: 0 auto 40px;
}
```

Research recommends offset pattern with nth-child selectors for alternating vertical offsets.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add asymmetric tutor card offsets</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update the `.trust-tutors` and `.tutor-card` CSS to create an asymmetric staggered layout:

1. Replace the existing `.trust-tutors` rule (around line 1628) with a fixed 5-column grid that allows nth-child positioning:

```css
.trust-tutors {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: var(--space-5);
    justify-content: center;
    max-width: 1100px;
    margin: 0 auto 40px;
}
```

2. Add nth-child offset rules for the tutor cards AFTER the existing `.tutor-card:hover` rule (around line 1650):

```css
/* Asymmetric staggered layout - editorial tension */
@media (min-width: 1001px) {
    .tutor-card:nth-child(2),
    .tutor-card:nth-child(4) {
        margin-top: var(--space-8);
    }
}
```

3. Add tablet breakpoint (901-1000px) - 4 columns with alternating offset:

```css
@media (min-width: 901px) and (max-width: 1000px) {
    .trust-tutors {
        grid-template-columns: repeat(4, 1fr);
    }
    .tutor-card:nth-child(2),
    .tutor-card:nth-child(4) {
        margin-top: var(--space-6);
    }
}
```

4. Update the existing 900px breakpoint (around line 4142) to use 2 columns and reset margins:

```css
@media (max-width: 900px) {
    .trust-tutors {
        grid-template-columns: repeat(2, 1fr);
    }
    .tutor-card {
        margin-top: 0 !important;
    }
}
```

5. Keep the existing 768px mobile breakpoint that sets `grid-template-columns: 1fr` (single column).

WHY: Alternating 2nd and 4th cards create visual rhythm without being chaotic. Reset on mobile ensures clean single-column stack.
  </action>
  <verify>
Open in browser at desktop width (1200px):
- Tutor cards 2 and 4 should be visually lower than cards 1, 3, and 5
- Creates a "wave" pattern across the row
- No horizontal overflow

Resize to tablet (900px):
- Grid collapses to 2 columns
- All cards align at top (no offset)

Resize to mobile (375px):
- Single column stack
- All cards full-width, no offset
  </verify>
  <done>Trust section has editorial staggered layout on desktop that degrades gracefully to standard grid on smaller screens.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no horizontal overflow introduced</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
After applying the asymmetric layout, verify no horizontal scroll is introduced:

1. Check the `.trust-tutors` max-width constraint is still present
2. Ensure the offset margins don't cause cards to extend beyond container
3. Test at various viewport widths

If horizontal overflow is detected, adjust by:
- Adding `overflow-x: hidden` to `.trust-section`
- Reducing `--space-8` offset to `--space-6` if needed

The research flagged this as Pitfall 1: "Asymmetry Causing Mobile Overflow" - so explicit verification is required.
  </action>
  <verify>
At all viewport widths from 320px to 1400px:
- No horizontal scrollbar appears
- Page width stays at 100vw
- Cards stay within their container bounds
  </verify>
  <done>Asymmetric layout does not cause horizontal overflow at any viewport width.</done>
</task>

</tasks>

<verification>
## Phase Verification

After completing tasks:

1. **Desktop (1200px+):** Trust section shows 5 tutor cards in a row with cards 2 and 4 offset downward
2. **Tablet (768-1000px):** Grid adapts to fewer columns, offsets may reduce or reset
3. **Mobile (320-767px):** Single column with no offsets
4. **No overflow:** Horizontal scroll never appears at any width
5. **Hover states:** Card hover still works (translateY doesn't conflict with offset)
</verification>

<success_criteria>
- [ ] Trust section has visible staggered card layout on desktop
- [ ] Layout creates editorial "visual tension" without being chaotic
- [ ] Mobile shows clean single-column stack
- [ ] No horizontal overflow at any viewport width
- [ ] Existing hover effects still work
</success_criteria>

<output>
After completion, create `.planning/phases/22-layout-mobile-conversion/22-01-SUMMARY.md`
</output>
