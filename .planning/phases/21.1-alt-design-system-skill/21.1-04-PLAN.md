---
phase: 21.1-alt-design-system-skill
plan: 04
type: execute
wave: 3
depends_on:
  - 21.1-03
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: false

must_haves:
  truths:
    - "All section headings use Fraunces display font"
    - "All body text uses Bricolage Grotesque"
    - "Section titles use consistent --text-4xl size"
    - "Card titles use consistent --text-xl size"
    - "Design tokens are used consistently throughout"
    - "Lime accent used sparingly (max 3 elements per viewport)"
    - "Page has cohesive editorial feel from top to bottom"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Complete design system application"
      contains: "font-family: var(--font-display)"
  key_links:
    - from: "section headings"
      to: "typography system"
      via: "CSS custom properties"
      pattern: "font-family.*var\\(--font-display\\)"
    - from: "body text"
      to: "typography system"
      via: "CSS custom properties"
      pattern: "font-family.*var\\(--font-body\\)"
---

<objective>
Apply the design system consistently to all remaining sections, creating a cohesive editorial experience throughout the page.

Purpose: Extend the distinctive typography and design tokens from the hero to all sections (courses, testimonials, pricing, about, contact, etc.). This ensures the page feels cohesive and the editorial aesthetic carries through the entire experience.

Output: Complete page with consistent design system application.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21.1-alt-design-system-skill/21.1-RESEARCH.md
@.planning/phases/21.1-alt-design-system-skill/21.1-01-SUMMARY.md
@.planning/phases/21.1-alt-design-system-skill/21.1-02-SUMMARY.md
@.planning/phases/21.1-alt-design-system-skill/21.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Section Titles to Use Fraunces</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Create a consistent section title style pattern and apply to all major sections.

Add this CSS for section titles (NOT tilted - only hero gets tilt):

```css
/* Section Titles - Fraunces without tilt */
.section-title,
section h2,
.courses h2,
.pricing h2,
.testimonials h2,
.about h2,
.contact h2,
.faq h2 {
    font-family: var(--font-display);
    font-size: var(--text-4xl);
    font-weight: 600;
    font-variation-settings: 'WONK' 0.5; /* Subtle wonk, not extreme */
    line-height: var(--leading-tight);
    letter-spacing: var(--tracking-tight);
    color: var(--gray-900);
    margin-bottom: var(--space-6);
}

/* Section title accent span (for colored words) */
.section-title-accent,
section h2 span,
.title-accent {
    color: var(--purple-700);
}
```

Scan the HTML for all section h2 elements and ensure they don't have conflicting inline styles that would override the display font.
  </action>
  <verify>Scroll through page. All section titles (Courses, Pricing, Testimonials, About, etc.) should render in Fraunces serif.</verify>
  <done>All section titles use Fraunces display font with consistent sizing.</done>
</task>

<task type="auto">
  <name>Task 2: Update Card and Component Typography</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Apply consistent typography to cards and repeated components:

```css
/* Card Titles - Smaller Fraunces */
.card-title,
.pricing-card h3,
.course-card h3,
.testimonial-name,
.feature-title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 500;
    line-height: var(--leading-snug);
    color: var(--gray-900);
}

/* Card Body Text - Bricolage */
.card-body,
.pricing-card p,
.course-card p,
.testimonial-text,
.feature-description {
    font-family: var(--font-body);
    font-size: var(--text-base);
    line-height: var(--leading-normal);
    color: var(--gray-600);
}

/* Pricing amounts */
.price,
.pricing-amount {
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--purple-700);
}

/* Labels and badges */
.badge,
.label,
.tag {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
}

/* Testimonial quotes */
.testimonial-quote,
blockquote {
    font-family: var(--font-body);
    font-size: var(--text-lg);
    font-style: italic;
    line-height: var(--leading-relaxed);
    color: var(--gray-700);
}
```

Adjust selectors based on actual class names in the HTML.
  </action>
  <verify>Check pricing cards, course cards, testimonials. Card titles should be in Fraunces, body text in Bricolage.</verify>
  <done>Cards and components use consistent typography from design system.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Lime Accent Sparingly to Key CTAs</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Add lime accent to 2-3 strategic secondary CTAs (max 3 lime elements per viewport).

RULE: Lime accent is for PRIMARY conversion actions only:
- Hero CTA (already done in Plan 03)
- Pricing "Get Started" buttons (most important conversion point)
- Final CTA section button (if exists)

DO NOT add lime to:
- Secondary actions ("Learn More", "View Details")
- Navigation
- Links in body text

Add the btn-lime class to pricing tier CTAs:
```html
<!-- In pricing cards, change primary buttons to btn-lime -->
<a href="#..." class="btn btn-lime">Get Started</a>
```

If there's a final CTA section at the bottom, also make that lime.

Keep all other buttons with existing styles (purple primary or outline).
  </action>
  <verify>Count lime-colored elements visible at any scroll position. Should be max 3. Hero has 1, pricing tier buttons should be the only others visible in their viewport.</verify>
  <done>Lime accent applied sparingly to 2-3 key conversion CTAs only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete design system application across all sections:
1. All section titles in Fraunces display font
2. All body text in Bricolage Grotesque
3. Consistent spacing using design tokens
4. Lime accent on strategic CTAs only
5. Cohesive editorial feel throughout
  </what-built>
  <how-to-verify>
1. Open C:/Users/bskim/Downloads/beamacademy-index.html in browser
2. Scroll through entire page slowly, checking each section:
   - Hero: Tilted Fraunces headline, lime CTA (from Plan 03)
   - Courses: Fraunces section title, Bricolage descriptions
   - Pricing: Fraunces title, Fraunces price amounts, lime "Get Started" buttons
   - Testimonials: Fraunces names, Bricolage quotes
   - About: Fraunces title, Bricolage body
   - Contact: Fraunces title, Bricolage form labels
3. Check lime accent usage:
   - Is lime used sparingly? (max 3 visible at once)
   - Is lime only on conversion-focused buttons?
4. Overall impression:
   - Does the page feel cohesive?
   - Does it feel like an editorial magazine about education?
   - Is it memorable and distinctive?
   - Would a parent (40-55) trust this site?
5. Mobile check (resize to 375px):
   - Typography still readable?
   - No cramped text or overflow?
  </how-to-verify>
  <resume-signal>Type "approved" if design system is consistently applied and page looks cohesive and distinctive, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. Typography audit:
   - All h2 section titles: Fraunces
   - All card titles: Fraunces
   - All body paragraphs: Bricolage Grotesque
   - Pricing amounts: Fraunces (bold)
2. Design token usage:
   - Spacing uses --space-X tokens
   - Colors use --color-X or --purple-X tokens
   - Shadows use --shadow-X tokens
3. Accent audit:
   - Lime elements per viewport <= 3
   - Lime only on primary CTAs
4. Consistency:
   - Same font sizes for same element types
   - Same spacing patterns across sections
5. Mobile responsive:
   - All sections work at 320px
   - Text readable without zooming
</verification>

<success_criteria>
- Page has unified editorial aesthetic from top to bottom
- Typography hierarchy is clear (display vs body fonts)
- Design tokens used consistently (not hardcoded values)
- Lime accent creates visual rhythm without overwhelming
- Page is distinctly memorable compared to generic tutoring sites
- Parent audience finds it trustworthy and professional
</success_criteria>

<output>
After completion, create `.planning/phases/21.1-alt-design-system-skill/21.1-04-SUMMARY.md`
</output>
