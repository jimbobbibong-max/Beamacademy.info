---
phase: 21.1-alt-design-system-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: true

must_haves:
  truths:
    - "CSS custom properties for colors are defined in :root"
    - "CSS custom properties for spacing scale are defined in :root"
    - "CSS custom properties for shadows are defined in :root"
    - "CSS custom properties for borders/radius are defined in :root"
    - "CSS custom properties for transitions are defined in :root"
    - "Existing page renders without visual regression"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Complete design tokens in :root"
      contains: "--color-lime-500"
  key_links:
    - from: ":root CSS"
      to: "design system"
      via: "CSS custom properties"
      pattern: "--color-purple-700.*--color-lime-500.*--space-.*--shadow-.*--radius-"
---

<objective>
Establish the complete CSS design tokens foundation for Phase 21.1's bold design system.

Purpose: Create the foundational design tokens (colors, spacing, shadows, borders, transitions) that all subsequent plans will reference. This enables consistent application of the editorial/magazine aesthetic with Fraunces + Bricolage typography and chartreuse accents.

Output: Updated :root CSS with complete design token system from research.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21.1-alt-design-system-skill/21.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Complete Color Palette Tokens</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Replace the existing color variables in the :root CSS block with the complete palette from research:

KEEP the existing purple-50 through purple-900 values (they match).

ADD the new lime accent colors:
```css
/* ACCENT: Electric Lime */
--color-lime-400: #d9ff00;
--color-lime-500: #bfff00;     /* Primary accent */
--color-lime-600: #a6ff00;
```

ADD semantic color aliases:
```css
/* SEMANTIC */
--color-primary: var(--color-purple-700);
--color-primary-dark: var(--color-purple-800);
--color-accent: var(--color-lime-500);
--color-accent-dark: var(--color-lime-600);
--color-text: var(--color-gray-800);
--color-text-muted: var(--color-gray-500);
--color-background: var(--white);
--color-surface: var(--gray-50);
```

Note: Keep existing --purple-X names for backwards compatibility but also add --color-purple-X aliases where needed. The existing CSS uses --purple-700 directly, so we keep that.
  </action>
  <verify>Search for "--color-lime-500" and "--color-accent" in the HTML file to confirm they exist in :root.</verify>
  <done>Complete color palette with lime accents and semantic aliases defined in :root.</done>
</task>

<task type="auto">
  <name>Task 2: Add Spacing, Shadow, Border, and Transition Tokens</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Add these design tokens to the :root CSS block (after colors):

SPACING (8px base):
```css
/* SPACING */
--space-1: 0.25rem;   /* 4px */
--space-2: 0.5rem;    /* 8px */
--space-3: 0.75rem;   /* 12px */
--space-4: 1rem;      /* 16px */
--space-5: 1.5rem;    /* 24px */
--space-6: 2rem;      /* 32px */
--space-8: 3rem;      /* 48px */
--space-10: 4rem;     /* 64px */
--space-12: 6rem;     /* 96px */
--space-16: 8rem;     /* 128px */
```

SHADOWS:
```css
/* SHADOWS */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.04);
--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.07);
--shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.08);
--shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
--shadow-purple: 0 8px 30px rgba(124, 58, 237, 0.2);
--shadow-lime: 0 4px 20px rgba(191, 255, 0, 0.3);
```

BORDERS:
```css
/* BORDERS */
--radius-sm: 4px;
--radius-md: 8px;
--radius-lg: 16px;
--radius-xl: 24px;
--radius-full: 9999px;
```

TRANSITIONS:
```css
/* TRANSITIONS */
--transition-fast: 150ms ease;
--transition-base: 250ms ease;
--transition-slow: 400ms ease;
```

Place these after the color definitions in :root, before --font-display.
  </action>
  <verify>Search for "--space-6", "--shadow-lime", "--radius-lg", and "--transition-base" in the HTML file.</verify>
  <done>Complete spacing scale, shadow variants, border radius scale, and transition tokens defined in :root.</done>
</task>

<task type="auto">
  <name>Task 3: Add Font Family Tokens (Placeholders)</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update the font custom properties in :root to prepare for Fraunces + Bricolage:

KEEP existing fonts working for now (Plan 02 will load new fonts):
```css
/* FONTS - Phase 21.1 Design System */
--font-display: 'Fraunces', Georgia, serif;
--font-body: 'Bricolage Grotesque', 'Outfit', system-ui, sans-serif;
```

Note: The existing page uses 'Outfit' so we include it as fallback for Bricolage until fonts load. The page will continue working with Outfit until Plan 02 loads the new fonts.

DO NOT change any existing font-family usage in CSS rules yet - that happens in Plan 02.
  </action>
  <verify>Search for "'Fraunces'" and "'Bricolage Grotesque'" in the :root section.</verify>
  <done>Font family tokens updated to reference new fonts with appropriate fallbacks.</done>
</task>

</tasks>

<verification>
1. Open C:/Users/bskim/Downloads/beamacademy-index.html in browser
2. Page should render identically to before (no visual changes yet - tokens are just defined)
3. Use browser DevTools to inspect :root and confirm all new custom properties exist:
   - Color tokens: --color-lime-500, --color-accent, --color-primary
   - Spacing tokens: --space-1 through --space-16
   - Shadow tokens: --shadow-sm through --shadow-lime
   - Border tokens: --radius-sm through --radius-full
   - Transition tokens: --transition-fast, --transition-base, --transition-slow
   - Font tokens: --font-display, --font-body
</verification>

<success_criteria>
- All design tokens from research are defined in :root
- Page renders without visual regression (existing styles still work)
- Tokens follow naming convention from 21.1-RESEARCH.md
- Foundation ready for Plan 02 (typography) and Plan 03 (hero transformation)
</success_criteria>

<output>
After completion, create `.planning/phases/21.1-alt-design-system-skill/21.1-01-SUMMARY.md`
</output>
