---
phase: 12-content-consolidation
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - C:/Users/bskim/beamacademy-site/index.html
autonomous: false

must_haves:
  truths:
    - "No orphaned .testimonials CSS remains"
    - "Trust section mobile layout works (320px-428px)"
    - "No broken internal links"
    - "Page visual check passes on mobile and desktop"
  artifacts:
    - path: "index.html"
      provides: "Final cleaned HTML with all consolidation complete"
  key_links:
    - from: "All href links"
      to: "Existing section IDs"
      via: "id attribute matching"
      pattern: "All #href targets have matching id= in document"
---

<objective>
Clean up remaining Testimonials CSS, verify all links work, and perform final mobile verification.

Purpose: Complete cleanup and ensure no orphaned CSS or broken links remain.
Output: Fully consolidated, clean HTML with mobile verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-content-consolidation/12-02-SUMMARY.md
@.planning/phases/12-content-consolidation/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Testimonials CSS (if orphaned)</name>
  <files>C:/Users/bskim/beamacademy-site/index.html</files>
  <action>
Check if `.testimonials` CSS rules are still used anywhere.

The Trust section uses `.trust-testimonial` (not `.testimonial-card`), so the standalone Testimonials section CSS should be orphaned.

Find and delete (around lines 2837-2910):

```css
        /* Testimonials */
        .testimonials { ... }
        .testimonials-grid { ... }
        .testimonial-card { ... }
        .testimonial-card::before { ... }
        .testimonial-card:hover { ... }
        .testimonial-quote { ... }
```

KEEP the following shared classes (used by Trust section):
- `.testimonial-author`
- `.testimonial-avatar`
- `.testimonial-name`
- `.testimonial-school`

Also check mobile rules:
- Around line 4073: `.testimonials-grid { ... }` - remove if orphaned

Update JS observer (should have been partially done in 12-03):
- Remove `.testimonial-card` from querySelectorAll if still present
  </action>
  <verify>
Run: grep -n "\.testimonials {" index.html
Expected: No matches

Run: grep -n "\.testimonial-author" index.html
Expected: Should still exist (used by Trust section)
  </verify>
  <done>Testimonials section CSS removed, shared testimonial-* classes preserved (MERGE-06)</done>
</task>

<task type="auto">
  <name>Task 2: Verify no broken internal links</name>
  <files>C:/Users/bskim/beamacademy-site/index.html</files>
  <action>
Verify all internal href="#..." links point to existing section IDs.

Extract all href="#..." values and verify each has a matching id="..." in the document.

Expected valid internal links:
- #home (hero section)
- #proof (proof section)
- #comparison (comparison section)
- #trust (trust section)
- #support (support section)
- #about (about section)
- #courses (courses-pricing section)
- #faq (faq section)
- #contact (contact section)

Invalid links that should NOT exist:
- #why-us (deleted)
- #portal (deleted)
- #team (deleted)
- #testimonials (never had one, but check)

Also check:
- Skip links (accessibility)
- Footer links
- Mobile nav links

If any broken links found, either remove the link or update to point to appropriate section.
  </action>
  <verify>
Run custom verification:
1. Extract all href="#..." values
2. Extract all id="..." values
3. Verify each href target exists as an id

All internal links should resolve to existing sections.
  </verify>
  <done>All internal links point to valid sections (QUAL-03)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete content consolidation:
- Trust section now has 4 tutors (Chris, Nathan, John, Taehoon)
- Trust section now has 3 testimonials (1 parent, 2 students)
- Why Us, Portal, Team, Testimonials sections removed
- All orphaned CSS cleaned up
- Navigation updated
  </what-built>
  <how-to-verify>
1. Open index.html in browser

2. Desktop verification (1200px+):
   - Scroll to Trust section - verify 4 tutor cards in row
   - Verify 3 testimonials below tutors (1 parent quote, 2 student quotes)
   - Check navigation - no "Why Us" or "Team" links
   - Verify smooth scroll to remaining sections works

3. Mobile verification (375px):
   - Use DevTools responsive mode at 375px width
   - Trust section tutors should stack (1 column) or 2x2 grid
   - Testimonials should stack vertically
   - No horizontal overflow
   - All text readable

4. Section flow check:
   - Scroll through entire page
   - Verify section order makes sense: Hero -> Proof -> Comparison -> Trust -> Support -> About -> Courses -> FAQ -> Contact -> CTA
   - No missing sections, no duplicates

5. Footer verification:
   - Check footer links work
   - No broken "Our Team" link
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `grep "\.testimonials {" index.html` returns nothing
2. `grep "testimonial-author" index.html` returns matches (preserved)
3. All href="#..." have matching id="..."
4. Human visual verification passes on mobile and desktop
5. No console errors when loading page
</verification>

<success_criteria>
- MERGE-06 complete (Testimonials CSS removed)
- QUAL-01 complete (mobile renders correctly)
- QUAL-03 complete (no broken links)
- QUAL-04 complete (no orphaned CSS)
- Human verification approves final result

ALL REQUIREMENTS COMPLETE:
- DEL-01 through DEL-04: Why Us and Portal removed
- MERGE-01 through MERGE-06: Content consolidated into Trust
- QUAL-01 through QUAL-04: Quality verified
</success_criteria>

<output>
After completion, create `.planning/phases/12-content-consolidation/12-04-SUMMARY.md`
</output>
