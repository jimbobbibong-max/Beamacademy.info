---
phase: 23-visual-polish-motion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: true

must_haves:
  truths:
    - "Hero headline has noticeably bolder rotation (more than -2deg)"
    - "Background has subtle visual texture for depth"
    - "Hero visual element breaks the grid container"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Visual differentiator CSS"
      contains: "rotate(-"
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Grain texture filter"
      contains: "feTurbulence"
  key_links:
    - from: "hero::after"
      to: "grain SVG filter"
      via: "pseudo-element with mix-blend-mode"
      pattern: "mix-blend-mode"
---

<objective>
Implement bold visual differentiators from Frontend Design Skill critique: increased hero tilt rotation, SVG grain texture overlay, and grid-breaking hero visual.

Purpose: Create distinctive, non-generic visual identity that breaks out of safe corporate patterns
Output: Hero section with bolder rotation (-4deg), subtle grain texture, and visual element that overflows container
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-visual-polish-motion/23-RESEARCH.md

Key research findings to apply:
- SVG feTurbulence for grain texture (no network requests)
- Use opacity 0.03-0.04 for subtle effect
- mix-blend-mode: multiply or overlay
- Grid-breaking via negative margins with overflow-x: hidden on parent
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase hero headline rotation to -4deg</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Find the .hero-title-climb class (or equivalent tilted headline) and increase rotation from -2deg to -4deg.

Research recommends -4deg as middle ground between "timid" (-2deg) and "aggressive" (-6deg).

CSS change:
```css
.hero-title-climb {
  /* Change from rotate(-2deg) to rotate(-4deg) */
  transform: rotate(-4deg);
}
```

WHY -4deg not -6deg: The research notes -6deg may feel too aggressive on smaller screens. -4deg provides noticeable boldness while maintaining readability across devices.
  </action>
  <verify>
Inspect the hero headline in browser DevTools and confirm transform: rotate(-4deg) is applied.
  </verify>
  <done>Hero headline has -4deg rotation, creating a noticeably bolder tilt than before</done>
</task>

<task type="auto">
  <name>Task 2: Add SVG grain texture to hero background</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Add subtle grain texture to hero section using inline SVG filter (no network request needed).

1. Add hidden SVG filter element near the top of the body (after opening body tag):

```html
<!-- Grain texture filter -->
<svg class="sr-only" aria-hidden="true" style="position:absolute;width:0;height:0;">
  <filter id="grain-filter">
    <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch"/>
    <feColorMatrix type="saturate" values="0"/>
  </filter>
</svg>
```

2. Add CSS for hero grain overlay using ::after pseudo-element:

```css
.hero {
  position: relative;
  /* Existing styles remain */
}

.hero::after {
  content: '';
  position: absolute;
  inset: 0;
  background: url('data:image/svg+xml,<svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg"><filter id="n"><feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch"/></filter><rect width="100%25" height="100%25" filter="url(%23n)"/></svg>');
  opacity: 0.04;
  pointer-events: none;
  mix-blend-mode: multiply;
  z-index: 1;
}
```

3. Ensure hero content has z-index higher than grain (z-index: 2 or higher).

WHY inline data URI: No network request, no external file dependency, works offline.
WHY 0.04 opacity: Visible enough for texture depth, subtle enough not to interfere with readability.
  </action>
  <verify>
1. View hero section in browser
2. Subtle grainy texture visible on hero background
3. Text remains readable (grain doesn't interfere)
4. Network tab shows no additional image requests for texture
  </verify>
  <done>Hero has subtle grain texture overlay that adds visual depth without affecting readability</done>
</task>

<task type="auto">
  <name>Task 3: Make hero visual break the grid</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Allow the hero visual element (mockup/image) to overflow its container boundary, creating asymmetric grid-breaking layout.

1. Find the hero visual container and add negative margin to break out:

```css
.hero-visual {
  position: relative;
  margin-right: -40px; /* Break out of container on right */
  /* Or margin-left: -40px for left breakout */
}

/* Mobile: reduce or remove breakout */
@media (max-width: 768px) {
  .hero-visual {
    margin-right: 0; /* No overflow on mobile */
  }
}
```

2. Ensure parent section has overflow-x: hidden to prevent horizontal scroll:

```css
.hero {
  overflow-x: hidden; /* Contain horizontal overflow */
}
```

WHY -40px: Noticeable breakout without being too aggressive. Adjust if needed.
WHY overflow-x:hidden: Prevents CLS and horizontal scroll issues.
WHY no mobile breakout: Mobile viewports can't afford lost content area.
  </action>
  <verify>
1. On desktop (>768px), hero visual extends beyond its container
2. No horizontal scrollbar appears
3. On mobile, hero visual stays within bounds
4. CLS test: No layout shift warnings
  </verify>
  <done>Hero visual breaks grid on desktop, creating asymmetric layout without causing horizontal scroll</done>
</task>

</tasks>

<verification>
- [ ] Hero headline rotation is -4deg (bolder than original -2deg)
- [ ] Grain texture visible on hero background (very subtle)
- [ ] Hero visual overflows container on desktop
- [ ] No horizontal scrollbar on any viewport
- [ ] Mobile layout unaffected by grid-breaking changes
- [ ] prefers-reduced-motion not needed for static visual changes
</verification>

<success_criteria>
Phase 23 Plan 01 complete when:
1. Hero has visually bolder tilt (-4deg rotation)
2. Subtle grain texture adds background depth
3. Grid-breaking visual creates distinctive asymmetric layout
4. No performance regressions (no layout thrashing, no CLS)
</success_criteria>

<output>
After completion, create `.planning/phases/23-visual-polish-motion/23-01-SUMMARY.md` using summary template.
</output>
