---
phase: 11-cta-mobile-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: false

must_haves:
  truths:
    - "Hero section renders without horizontal scroll at 320px"
    - "Proof section document grid collapses to single column on mobile"
    - "Comparison section shows cards (not table) on mobile"
    - "Trust section tutor grid collapses appropriately"
    - "Support section Discord mockup is readable at 320px"
    - "Sticky CTA bar appears on scroll and buttons are tappable"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Mobile-optimized CSS if fixes needed"
      min_lines: 7100
  key_links:
    - from: "Sticky CTA bar"
      to: "#contact form"
      via: "href=#contact + scroll behavior"
      pattern: "sticky-cta-button"
---

<objective>
Test all new sections (phases 6-10) at mobile viewports (320px, 375px, 428px) and verify sticky CTA bar functionality with the new page structure.

Purpose: Ensure the messaging overhaul content from phases 6-10 displays correctly on the smallest mobile devices. The research identified potential overflow risks at 320px.

Output: Verified mobile rendering OR CSS fixes applied for any issues found.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/bskim/beamacademy-site/.planning/PROJECT.md
@C:/Users/bskim/beamacademy-site/.planning/ROADMAP.md
@C:/Users/bskim/beamacademy-site/.planning/phases/11-cta-mobile-polish/11-RESEARCH.md

Target file:
@C:/Users/bskim/Downloads/beamacademy-index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze mobile CSS and identify potential issues</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Examine the existing CSS media queries for the 6 sections added in phases 6-10:

1. **Hero section** (lines ~5617-5687)
   - Check `.hero-mockup-tags` flex behavior at 768px breakpoint
   - Check `.weak-tag` font-size (0.75rem) readability
   - Check portal mockup width constraints

2. **Proof section** (lines ~5690-5814)
   - Check `.documents-grid` responsive behavior
   - Check `.document-preview` height (160px on mobile)
   - Check `.paper-mockup` font-size (0.7rem) - may be too small

3. **Comparison section** (lines ~5816-5897)
   - Verify table hidden, cards shown on mobile
   - Check `.comparison-card` max-width (500px)

4. **Trust section** (lines ~5899-5986)
   - Check `.tutors-grid` column behavior (3 -> 2 -> 1)
   - Check `.trifecta-badges` font-size (0.65rem)

5. **Support section** (lines ~5988-6080)
   - Check Discord mockup responsiveness
   - Check side-by-side layout collapse

6. **Sticky CTA bar** (lines ~7059-7068)
   - Verify z-index and positioning
   - Check button touch target sizes (44px minimum)

Document any CSS that may cause issues at 320px width.
  </action>
  <verify>
Review complete - document findings for each section:
- Section name
- Current breakpoints used
- Potential issues at 320px
- Recommended fixes (if any)
  </verify>
  <done>
CSS analysis complete with documented findings for each section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply preventive CSS fixes for 320px viewport</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Based on research findings, add a new media query block for ultra-narrow viewports (320px-375px) to address these known risks:

1. **Add @media (max-width: 375px) block** in the style section, after the existing 600px breakpoint:

```css
/* Ultra-narrow mobile (iPhone SE and similar) */
@media (max-width: 375px) {
    /* Proof section - prevent text from being too small */
    .paper-mockup {
        font-size: 0.65rem;
    }

    .proof-badge {
        font-size: 0.7rem;
        padding: 0.4rem 0.8rem;
    }

    /* Hero mockup - ensure weak tags don't overflow */
    .weak-tag {
        font-size: 0.65rem;
        padding: 0.2rem 0.4rem;
    }

    /* Trust section - smaller badges */
    .trifecta-badges span {
        font-size: 0.6rem;
    }

    /* Ensure sticky bar buttons remain tappable */
    .sticky-cta-button {
        min-height: 44px;
        font-size: 0.9rem;
    }
}
```

2. **Verify existing min-width/min-height constraints** on interactive elements maintain 44px minimum for touch targets.

3. **Add overflow-x: hidden** to body if not already present, to prevent accidental horizontal scroll.

IMPORTANT: Only add CSS that prevents issues. Do not over-engineer or change things that already work.
  </action>
  <verify>
- Grep for "@media (max-width: 375px)" to confirm new breakpoint added
- Check that existing 768px and 600px breakpoints remain intact
- Verify no syntax errors in CSS
  </verify>
  <done>
Preventive CSS fixes applied for 320px-375px viewport range. Existing breakpoints preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Mobile responsive fixes for all new sections (Hero, Proof, Comparison, Trust, Support) and verified sticky CTA bar functionality.
  </what-built>
  <how-to-verify>
**Test in Chrome DevTools at each viewport width:**

Open C:/Users/bskim/Downloads/beamacademy-index.html in Chrome, press F12, click device toolbar (Ctrl+Shift+M), and test at these widths:

**At 320px (iPhone SE):**
1. Hero section: Portal mockup visible, no horizontal scroll, weak tags readable
2. Proof section: Documents stack single-column, text readable
3. Comparison section: Cards visible (not table), no overflow
4. Trust section: Tutor cards stack, badges visible
5. Support section: Discord mockup readable
6. Scroll down: Sticky CTA bar appears after hero, both buttons tappable

**At 375px (iPhone 12/13/14):**
1. Same checks as 320px
2. Verify slightly larger text is still well-proportioned

**At 428px (iPhone 14 Pro Max):**
1. Same checks
2. Verify content doesn't look too cramped

**Sticky CTA bar test:**
1. Scroll past hero section
2. Confirm bar appears at bottom
3. Tap "Get Your Free Diagnostic" (if 11-01 complete) or "Book Free Trial"
4. Confirm smooth scroll to contact form
5. Tap phone icon - confirm it opens phone dialer
  </how-to-verify>
  <resume-signal>
Type "approved" if all sections render correctly and sticky bar functions properly.

If issues found, describe:
- Which section(s) have problems
- At which viewport width(s)
- What the issue looks like (overflow, unreadable text, broken layout, etc.)
  </resume-signal>
</task>

</tasks>

<verification>
After completing all tasks:

1. **MOB-01 - Section rendering:**
   - All 6 sections (Hero, Proof, Comparison, Trust, Support, CTA) display correctly at 320px, 375px, 428px
   - No horizontal scroll on any viewport
   - All text readable (minimum 0.6rem / ~10px)
   - All grids collapse appropriately

2. **MOB-02 - Image loading:**
   - Verify all images in new sections use loading="lazy" (already confirmed in research)
   - No new images that could regress LCP

3. **MOB-03 - Sticky CTA bar:**
   - Appears on scroll past hero
   - Both buttons (Call, CTA) are tappable (44px+ touch targets)
   - Smooth scroll to form works
   - Bar hides on desktop (min-width: 769px)
</verification>

<success_criteria>
1. All new sections render without issues at 320px, 375px, 428px viewports
2. No horizontal scroll at any viewport width
3. Touch targets remain >= 44px on all interactive elements
4. Sticky CTA bar functional: appears on scroll, buttons work, smooth scroll to form
5. Human verification confirms acceptable mobile experience
</success_criteria>

<output>
After completion, create `.planning/phases/11-cta-mobile-polish/11-02-SUMMARY.md`
</output>
