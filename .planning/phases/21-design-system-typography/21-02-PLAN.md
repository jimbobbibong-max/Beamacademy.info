---
phase: 21-design-system-typography
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: true

must_haves:
  truths:
    - "Page loads Geist Sans font from Vercel CDN"
    - "Fluid typography scale responds to viewport width"
    - "Body text is readable at 40-55 age range (16-18px base)"
    - "Heading hierarchy is clear and consistent"
    - "Font loads without blocking page render (swap behavior)"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Geist font loading and fluid type scale"
      contains: "geistfont.vercel.app, --text-base, clamp("
  key_links:
    - from: "<head> section"
      to: "Geist font CDN"
      via: "preload link element"
      pattern: "geistfont\\.vercel\\.app"
    - from: ":root CSS block"
      to: "Typography tokens"
      via: "CSS custom property definitions"
      pattern: "--text-|--font-|--leading-"
    - from: "body, h1-h6 CSS rules"
      to: "Typography tokens"
      via: "var() references"
      pattern: "var\\(--text-|var\\(--font-"
---

<objective>
Implement Geist Sans typography system with fluid type scale using clamp().

Purpose: Replace the generic Outfit font with Geist Sans (Vercel's distinctive font) and implement a proper fluid typography scale that maintains readability across all viewport sizes, especially for the 40-55 age parent audience.

Output: Updated beamacademy-index.html with Geist font loading, fluid type tokens, and applied typography across the page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-design-system-typography/21-CONTEXT.md
@.planning/phases/21-design-system-typography/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Geist font loading to head</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Replace the existing Google Fonts loading (Outfit + Source Serif 4) with Geist Sans from Vercel CDN.

Find these lines in `<head>`:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="preload" href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Source+Serif+4:opsz,wght@8..60,400;8..60,500;8..60,600&display=swap" as="style" onload="this.onload=null;this.rel='stylesheet'">
<noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Source+Serif+4:opsz,wght@8..60,400;8..60,500;8..60,600&display=swap"></noscript>
```

Replace with:
```html
<!-- Geist Font (Vercel) -->
<link rel="preconnect" href="https://geistfont.vercel.app" crossorigin>
<link rel="preload" href="https://geistfont.vercel.app/geist.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
<noscript><link rel="stylesheet" href="https://geistfont.vercel.app/geist.css"></noscript>
```

Keep the Google Tag Manager preconnect (separate concern).
  </action>
  <verify>
Search the HTML file for "geistfont.vercel.app". Should find 3 occurrences (preconnect, preload href, noscript href).
  </verify>
  <done>
Geist font CSS loads from Vercel CDN with preconnect and preload for performance. Old Google Fonts references removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add typography tokens to :root</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Add typography tokens to `:root` block. Place these AFTER the spacing tokens and BEFORE the color tokens:

```css
/* =====================
   TYPOGRAPHY
   ===================== */
/* Font family */
--font-primary: 'Geist', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;

/* Font weights */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;

/* Fluid Type Scale (clamp: min, preferred, max) */
--text-xs: clamp(0.75rem, 0.72rem + 0.15vw, 0.84rem);     /* 12-13.5px */
--text-sm: clamp(0.875rem, 0.84rem + 0.17vw, 0.975rem);   /* 14-15.6px */
--text-base: clamp(1rem, 0.95rem + 0.25vw, 1.125rem);     /* 16-18px */
--text-lg: clamp(1.125rem, 1.06rem + 0.33vw, 1.313rem);   /* 18-21px */
--text-xl: clamp(1.25rem, 1.15rem + 0.5vw, 1.5rem);       /* 20-24px */
--text-2xl: clamp(1.5rem, 1.35rem + 0.75vw, 1.875rem);    /* 24-30px */
--text-3xl: clamp(1.875rem, 1.65rem + 1.13vw, 2.5rem);    /* 30-40px */
--text-4xl: clamp(2.25rem, 1.9rem + 1.75vw, 3.25rem);     /* 36-52px */
--text-5xl: clamp(2.75rem, 2.2rem + 2.75vw, 4.25rem);     /* 44-68px */

/* Line heights */
--leading-tight: 1.2;     /* Headings */
--leading-snug: 1.35;     /* Subheadings */
--leading-normal: 1.6;    /* Body text */
--leading-relaxed: 1.75;  /* Long-form reading */
```

Also update the existing font variables to point to the new tokens:
- Change `--font-display: 'Outfit', sans-serif;` to `--font-display: var(--font-primary);`
- Change `--font-body: 'Outfit', sans-serif;` to `--font-body: var(--font-primary);`
  </action>
  <verify>
Search for "--text-base" and "--font-primary" and "--leading-normal" in the file. All should exist in :root.
  </verify>
  <done>
Typography tokens exist: font family (Geist), 4 font weights, 9-step fluid type scale (xs to 5xl), and 4 line heights.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply typography tokens to base elements</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update the base element styles to use the new typography tokens.

Find and update the `body` rule:
```css
body {
    font-family: var(--font-body);
    font-size: var(--text-base);
    color: var(--gray-800);
    line-height: var(--leading-normal);
    background: var(--white);
    -webkit-font-smoothing: antialiased;
}
```

Find and update the heading rule (h1-h6):
```css
h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-display);
    font-weight: var(--font-semibold);
    line-height: var(--leading-tight);
}
```

Add individual heading size rules if not present (add after the h1-h6 block):
```css
h1 { font-size: var(--text-5xl); font-weight: var(--font-bold); }
h2 { font-size: var(--text-4xl); }
h3 { font-size: var(--text-3xl); }
h4 { font-size: var(--text-2xl); }
h5 { font-size: var(--text-xl); }
h6 { font-size: var(--text-lg); }
```

NOTE: These are BASE styles. Many sections override these with specific classes, which is fine. The tokens provide the foundation.
  </action>
  <verify>
1. Open the HTML file in browser
2. Inspect the body element - should show Geist font family
3. Inspect any h1 - should show font-size using clamp()
4. Resize browser window - text sizes should fluidly scale
  </verify>
  <done>
Body uses --text-base (16-18px fluid), headings use appropriate scale steps (h1=5xl, h2=4xl, etc.), line heights are token-based.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Open beamacademy-index.html in browser
2. Verify Geist font loads (check Network tab for geistfont.vercel.app request)
3. Verify no FOIT (text visible immediately, may flash when font swaps)
4. Resize browser from 360px to 1400px - text should scale smoothly
5. Body text should be comfortable to read (not too small)
6. Run Lighthouse - LCP should remain under 2.5s (target: maintain ~0.9s)

Requirements verified:
- TYP-01: Replaced generic Outfit with distinctive Geist Sans
- TYP-02: Single font (Geist) for both headings and body, varied by weight
- TYP-03: Fluid type scale using clamp() for responsive sizing
- TYP-04: Base text 16-18px readable for 40-55 age range
- DES-04: CSS custom properties for typography scale
</verification>

<success_criteria>
- Geist Sans loads from Vercel CDN
- Font preloaded for performance
- Fallback stack includes system fonts
- Fluid type scale with 9 steps (xs to 5xl)
- clamp() used for all type sizes
- Body text 16-18px (readable for parents)
- Headings use appropriate scale steps
- Line heights defined and applied
- LCP not significantly regressed
- Text remains readable at all viewport sizes
</success_criteria>

<output>
After completion, create `.planning/phases/21-design-system-typography/21-02-SUMMARY.md`
</output>
