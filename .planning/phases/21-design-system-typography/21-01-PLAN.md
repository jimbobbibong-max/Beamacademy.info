---
phase: 21-design-system-typography
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: true

must_haves:
  truths:
    - "CSS custom properties exist for all spacing values (8px grid)"
    - "CSS custom properties exist for complete color palette"
    - "CSS custom properties exist for shadow scale"
    - "CSS custom properties exist for border radius scale"
    - "CSS custom properties exist for transitions"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Design token CSS custom properties in :root"
      contains: "--space-1, --space-2, --color-primary, --shadow-sm"
  key_links:
    - from: ":root CSS block"
      to: "Design token variables"
      via: "CSS custom property definitions"
      pattern: "--space-\\d+:|--color-|--shadow-|--radius-"
---

<objective>
Establish CSS design tokens (custom properties) for spacing, colors, shadows, and utility values.

Purpose: Create the foundational design system that Phase 21-02 (typography) and 21-03 (visual differentiator) build upon. These tokens enable consistent, maintainable styling across the entire page.

Output: Updated `:root` block in beamacademy-index.html with complete design token system following 8px grid spacing, semantic color naming, and layered shadow scale.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-design-system-typography/21-CONTEXT.md
@.planning/phases/21-design-system-typography/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spacing scale tokens to :root</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Add the spacing scale CSS custom properties at the TOP of the existing `:root` block (before the existing color variables). Use 8px grid system with rem units for accessibility:

```css
/* =====================
   SPACING SCALE (8px grid)
   ===================== */
--space-1: 0.25rem;   /* 4px */
--space-2: 0.5rem;    /* 8px */
--space-3: 0.75rem;   /* 12px */
--space-4: 1rem;      /* 16px */
--space-5: 1.5rem;    /* 24px */
--space-6: 2rem;      /* 32px */
--space-8: 3rem;      /* 48px */
--space-10: 4rem;     /* 64px */
--space-12: 6rem;     /* 96px */
```

Keep existing variables below this new block. Do NOT remove any existing variables yet - migration happens in later tasks.
  </action>
  <verify>
Search the HTML file for "--space-1" and "--space-12" to confirm all spacing tokens are present. Should find 9 spacing variables total.
  </verify>
  <done>
Spacing scale tokens (--space-1 through --space-12) exist in :root block with proper rem values and comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add semantic color tokens and accent color</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Add semantic color tokens AFTER the existing purple and gray color scales in :root. These map to the existing palette but provide semantic meaning:

```css
/* =====================
   SEMANTIC COLORS
   ===================== */
--color-primary: var(--purple-700);
--color-primary-hover: var(--purple-800);
--color-primary-light: var(--purple-100);
--color-text: var(--gray-800);
--color-text-muted: var(--gray-500);
--color-text-inverted: var(--white);
--color-background: var(--white);
--color-background-alt: var(--gray-50);
--color-border: var(--gray-200);

/* Accent (warm orange for visual pop against purple) */
--color-accent: #f97316;
--color-accent-hover: #ea580c;
```

Keep all existing purple-* and gray-* variables - they're now the foundation that semantic colors reference.
  </action>
  <verify>
Search for "--color-primary" and "--color-accent" in the file. Both should exist in :root.
  </verify>
  <done>
Semantic color tokens exist including --color-primary, --color-text, --color-background, and --color-accent (#f97316).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add shadow scale, radius scale, and transition tokens</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Replace the existing shadow variables with a more complete scale, and add radius + transition tokens. Find the current shadow block (--shadow-sm, --shadow-md, etc.) and replace with:

```css
/* =====================
   SHADOW SCALE
   ===================== */
--shadow-xs: 0 1px 2px rgba(0, 0, 0, 0.04);
--shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.06), 0 1px 2px rgba(0, 0, 0, 0.04);
--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.07), 0 2px 4px -1px rgba(0, 0, 0, 0.04);
--shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.08), 0 4px 6px -2px rgba(0, 0, 0, 0.04);
--shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
--shadow-2xl: 0 25px 50px -12px rgba(0, 0, 0, 0.15);

/* Brand shadows (purple tinted) */
--shadow-purple-sm: 0 4px 14px rgba(124, 58, 237, 0.12);
--shadow-purple-md: 0 8px 25px rgba(124, 58, 237, 0.18);
--shadow-purple-lg: 0 16px 40px rgba(124, 58, 237, 0.22);

/* Keep existing special shadows */
--shadow-glow: 0 0 80px rgba(124, 58, 237, 0.25);
--shadow-card: 0 4px 20px rgba(0, 0, 0, 0.06);
--shadow-card-hover: 0 25px 80px rgba(124, 58, 237, 0.18);

/* =====================
   BORDER RADIUS SCALE
   ===================== */
--radius-sm: 4px;
--radius-md: 8px;
--radius-lg: 12px;
--radius-xl: 16px;
--radius-2xl: 24px;
--radius-full: 9999px;

/* =====================
   TRANSITIONS
   ===================== */
--transition-fast: 150ms ease;
--transition-base: 200ms ease;
--transition-slow: 300ms ease;
--transition-bounce: 300ms cubic-bezier(0.34, 1.56, 0.64, 1);
```

Remove --glass-bg and --glass-border if still present (not used in new design direction).
  </action>
  <verify>
Search for "--shadow-2xl", "--radius-lg", and "--transition-bounce" in the file. All three should exist in :root.
  </verify>
  <done>
Complete shadow scale (xs through 2xl + purple variants), radius scale (sm through full), and transition tokens exist in :root.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Open beamacademy-index.html in browser
2. Open DevTools > Elements > Computed Styles
3. Select :root element
4. Verify all custom properties are present and valid:
   - 9 spacing tokens (--space-1 through --space-12)
   - 11+ semantic color tokens (--color-primary, --color-accent, etc.)
   - 12+ shadow tokens (--shadow-xs through --shadow-2xl, purple variants)
   - 6 radius tokens (--radius-sm through --radius-full)
   - 4 transition tokens

Requirements verified:
- DES-01: Spacing scale exists (8, 16, 24, 32, 48, 64, 96px via rem)
- DES-02: Color palette tokens exist (purple, accent, neutrals)
- DES-03: Shadow scale exists (sm, md, lg, xl + 2xl + purple variants)
</verification>

<success_criteria>
- `:root` block contains complete design token system
- Spacing uses 8px grid with rem units
- Semantic color tokens reference existing palette
- Accent color #f97316 (warm orange) defined
- Shadow scale includes purple-tinted brand variants
- Radius and transition scales defined
- Page still renders correctly (no CSS errors)
- No visual regressions (existing styles still work)
</success_criteria>

<output>
After completion, create `.planning/phases/21-design-system-typography/21-01-SUMMARY.md`
</output>
