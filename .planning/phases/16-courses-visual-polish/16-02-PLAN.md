---
phase: 16-courses-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: true

must_haves:
  truths:
    - "Courses section has subtle purple gradient decoration in background"
    - "Course icons have subtle glow effect"
    - "Active year tab has shadow for emphasis"
    - "Tier CTAs and course buttons have shadow and gradient on hover"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Background decoration, icon glow, tab shadow, button enhancements"
      contains: "courses-pricing::before"
  key_links:
    - from: ".courses-pricing"
      to: "::before pseudo-element"
      via: "radial-gradient background"
      pattern: "courses-pricing::before.*radial-gradient"
    - from: ".tier-cta:hover"
      to: "button styling"
      via: "gradient and shadow"
      pattern: "\\.tier-cta:hover.*box-shadow"
---

<objective>
Add supporting visual polish: background decoration, icon glow, tab enhancement, and button consistency.

Purpose: These medium-impact improvements complete the v2 visual quality. Background decoration adds depth like Hero/About sections. Icon glow draws attention. Tab shadow improves active state. Button shadows match Hero CTA quality.

Output: Enhanced background, icon, tab, and button CSS in the Courses section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/bskim/beamacademy-site/.planning/PROJECT.md
@C:/Users/bskim/beamacademy-site/.planning/ROADMAP.md
@C:/Users/bskim/beamacademy-site/.planning/phases/16-courses-visual-polish/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add background decoration to Courses section</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Find the `.courses-pricing` rule (around line 1919). Update it to add `position: relative;` if not present, then add a ::before pseudo-element for the background decoration.

After the `.courses-pricing` rule, add:

```css
.courses-pricing::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 50%;
    height: 40%;
    background: radial-gradient(ellipse at top right, rgba(124, 58, 237, 0.06) 0%, transparent 60%);
    pointer-events: none;
    z-index: 0;
}
```

IMPORTANT: Keep opacity very low (0.06) to avoid "neon/gamer" aesthetic. This is subtle ambient lighting.

Also ensure all content inside .courses-pricing has `position: relative; z-index: 1;` OR add a wrapper. The simplest approach is to add to the section's container:

Find `.courses-pricing .container` or the container inside and add `position: relative; z-index: 1;` to ensure content stays above decoration.
  </action>
  <verify>
1. Open browser and scroll to Courses section
2. Look at top-right corner - should see VERY subtle purple glow
3. Glow should NOT be distracting or look "neon"
4. All content (tiers, cards) should be above the decoration (z-index)
5. No horizontal scrollbar introduced
  </verify>
  <done>
- Courses section has subtle purple radial gradient in top-right
- Decoration is barely visible (opacity 0.06)
- All content remains above decoration
- No overflow issues
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance course icons and year tabs</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Find the `.course-icon` rule (around line 2477). Add a subtle glow:
```css
box-shadow: 0 4px 12px rgba(168, 85, 247, 0.2);
```

Find the `.year-tab.active` rule (around line 2403). Add shadow for emphasis:
```css
box-shadow: 0 4px 12px rgba(124, 58, 237, 0.3);
```

These are subtle enhancements that add visual interest without being distracting.
  </action>
  <verify>
1. Scroll to "Browse Our Courses" section
2. Check course card icons (emoji containers) - should have subtle purple glow
3. Check year tabs - active tab should have visible purple shadow
4. Click different tabs - shadow should follow active state
  </verify>
  <done>
- Course icons have subtle purple glow (0.2 opacity)
- Active year tab has visible purple shadow
- Shadow properly follows active tab state
  </done>
</task>

<task type="auto">
  <name>Task 3: Add button gradient and shadow consistency</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Find the `.tier-cta` rule (around line 2165). Add base shadow:
```css
box-shadow: 0 2px 8px rgba(124, 58, 237, 0.2);
```

Update the `.tier-cta:hover` rule (around line 2178):
```css
.tier-cta:hover {
    background: linear-gradient(135deg, var(--purple-600), var(--purple-700));
    border-color: var(--purple-600);
    color: white;
    box-shadow: 0 4px 14px rgba(124, 58, 237, 0.4);
}
```

Find the `.course-btn` rule (around line 2523). Update to add gradient and shadow:
```css
.course-btn {
    padding: 10px 20px;
    border-radius: 100px;
    font-family: var(--font-display);
    font-size: 0.85rem;
    font-weight: 600;
    background: linear-gradient(135deg, var(--purple-600), var(--purple-700));
    color: white;
    border: none;
    cursor: pointer;
    transition: all 0.2s ease;
    text-decoration: none;
    box-shadow: 0 2px 8px rgba(124, 58, 237, 0.3);
}
```

Update `.course-btn:hover` rule:
```css
.course-btn:hover {
    background: linear-gradient(135deg, var(--purple-500), var(--purple-600));
    box-shadow: 0 4px 14px rgba(124, 58, 237, 0.4);
}
```

This matches the Hero section button styling.
  </action>
  <verify>
1. Scroll to pricing tiers - check "Get Your Free Diagnostic" CTAs
2. Tier CTAs should have subtle shadow at rest
3. Hover tier CTA - should show gradient background and stronger glow
4. Scroll to course cards - check "Enroll" buttons
5. Course buttons should have gradient and shadow at rest
6. Hover course button - shadow should intensify
  </verify>
  <done>
- Tier CTAs have base shadow and gradient+glow on hover
- Course buttons have gradient background and shadow
- Button styling matches Hero section quality
- All hover transitions are smooth
  </done>
</task>

</tasks>

<verification>
1. Background: Subtle purple gradient visible in top-right of Courses section
2. Icons: Course card icons have purple glow
3. Tabs: Active year tab has shadow emphasis
4. Buttons: All CTAs have shadows and gradients
5. Mobile: Check decorations don't cause issues at mobile breakpoints
6. Performance: No visible jank or performance degradation
</verification>

<success_criteria>
- [ ] Courses section has ::before decoration with radial-gradient
- [ ] Decoration opacity is 0.06 (subtle, not neon)
- [ ] Course icons have box-shadow glow
- [ ] Active year tab has purple shadow
- [ ] Tier CTAs have base shadow and hover gradient
- [ ] Course buttons have gradient and shadow
- [ ] All content has proper z-index above decoration
- [ ] No overflow or layout issues
</success_criteria>

<output>
After completion, create `.planning/phases/16-courses-visual-polish/16-02-SUMMARY.md`
</output>
