---
phase: 25-google-ads-saas-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: true
user_setup:
  - service: google-ads
    why: "Call tracking conversion action"
    env_vars: []
    dashboard_config:
      - task: "Create 'Calls from Website' conversion action"
        location: "Google Ads → Tools & Settings → Conversions → + New conversion action → Phone calls → Calls to phone number on website"
        values:
          phone_number: "0491 791 447"
          min_call_duration: "60 seconds"
      - task: "Note Conversion ID and Conversion Label"
        location: "Generated after creating conversion action"

must_haves:
  truths:
    - "Phone number is visible at top of page above navigation"
    - "Phone number is clickable on mobile (tel: link)"
    - "Utility bar stays fixed on scroll"
    - "Google Ads call tracking script is present"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Utility bar with click-to-call + call tracking"
      contains: "utility-bar"
  key_links:
    - from: "utility-bar phone link"
      to: "tel:+61491791447"
      via: "href attribute"
      pattern: 'href="tel:'
    - from: "gtag config"
      to: "phone_conversion_number"
      via: "gtag call tracking config"
      pattern: "phone_conversion"
---

<objective>
Add sticky utility bar with click-to-call phone number and Google Ads call tracking.

Purpose: Enable Google Ads "Calls from Website" conversion tracking and make phone number prominently visible per POP recommendations.
Output: Homepage with utility bar above nav, click-to-call functionality, and call tracking script ready for conversion ID.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-google-ads-saas-redesign/25-CONTEXT.md
@.planning/phases/25-google-ads-saas-redesign/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add utility bar HTML and CSS</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Add a utility bar component ABOVE the existing nav element. The utility bar should:

**HTML structure (insert between skip-link and nav):**
```html
<!-- Utility Bar - Click to Call -->
<div class="utility-bar">
  <div class="container">
    <a href="tel:+61491791447" class="phone-link">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
      </svg>
      <span class="phone-number">0491 791 447</span>
    </a>
  </div>
</div>
```

**CSS (add to existing styles, before .nav styles):**
```css
/* =====================
   UTILITY BAR (Phase 25)
   Slim bar above nav with click-to-call
   ===================== */
.utility-bar {
    position: sticky;
    top: 0;
    z-index: 1001;
    background: var(--gray-900);
    color: var(--white);
    padding: var(--space-2) 0;
    font-size: var(--text-sm);
}

.utility-bar .container {
    display: flex;
    justify-content: flex-end;
    align-items: center;
}

.phone-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    color: var(--white);
    text-decoration: none;
    font-family: var(--font-body);
    font-weight: 500;
    padding: var(--space-2) var(--space-3);
    border-radius: var(--radius-full);
    transition: background var(--transition-fast);
    min-height: 44px;
}

.phone-link:hover {
    background: rgba(255, 255, 255, 0.1);
}

.phone-link svg {
    flex-shrink: 0;
}

/* Mobile: Center phone, ensure tap target */
@media (max-width: 768px) {
    .utility-bar .container {
        justify-content: center;
    }

    .phone-link {
        flex: 1;
        justify-content: center;
        max-width: 280px;
    }
}
```

**Update nav positioning:**
The nav currently has `top: 0`. Update to account for utility bar height:
- Change `.nav { top: 0; }` to `.nav { top: 44px; }` 
- Update `.mobile-nav { top: 72px; }` to `.mobile-nav { top: 116px; }` (72 + 44)
  </action>
  <verify>
1. Open beamacademy-index.html in browser
2. Verify utility bar appears at very top with phone number
3. Verify nav appears below utility bar
4. Test phone link is clickable (on mobile should open dialer)
5. Scroll page - verify both utility bar and nav stay fixed
  </verify>
  <done>Utility bar displays above nav with centered phone on mobile, right-aligned on desktop. Click-to-call link works. Both bars sticky on scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Add Google Ads call tracking configuration</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Extend the existing gtag configuration to include phone call conversion tracking.

**Find the existing gtag code (around line 108-115):**
```javascript
gtag('js', new Date());
gtag('config', 'AW-17832807956');
```

**Replace with enhanced version:**
```javascript
gtag('js', new Date());
gtag('config', 'AW-17832807956');

// Phone call conversion tracking (Phase 25)
// Note: Replace CONVERSION_LABEL with actual label from Google Ads dashboard
// after creating "Calls from Website" conversion action
gtag('config', 'AW-17832807956/CONVERSION_LABEL', {
    'phone_conversion_number': '0491 791 447',
    'phone_conversion_css_class': 'phone-number'
});
```

**Add the phone-number class to the span:**
Ensure the phone number span in the utility bar has class="phone-number" (already specified in Task 1, verify it's there).

**IMPORTANT:** The CONVERSION_LABEL placeholder will be replaced by the user after they create the conversion action in Google Ads. Add a comment explaining this.
  </action>
  <verify>
1. View page source, confirm gtag config includes phone_conversion_number
2. Verify phone-number class is on the span element
3. Test with #google-wcc-debug appended to URL (if conversion is set up, numbers will show as 9999999999)
  </verify>
  <done>Google Ads call tracking script configured with phone number and CSS class targeting. Ready for conversion label from Google Ads dashboard.</done>
</task>

</tasks>

<verification>
1. Utility bar visible at top of page with phone number
2. Phone number clickable (tel: link)
3. Nav positioned correctly below utility bar
4. Both bars sticky on scroll
5. Mobile: phone centered, 44px tap target
6. Desktop: phone right-aligned
7. gtag call tracking config present in source
</verification>

<success_criteria>
- Phone number prominently visible at page top
- Click-to-call works on mobile devices
- Google Ads call tracking script ready for conversion ID
- No visual regressions to existing nav
- Utility bar respects BEAM design system (colors, fonts, spacing)
</success_criteria>

<output>
After completion, create `.planning/phases/25-google-ads-saas-redesign/25-01-SUMMARY.md`
</output>
