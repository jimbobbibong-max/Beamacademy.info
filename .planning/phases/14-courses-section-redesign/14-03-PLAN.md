---
phase: 14-courses-section-redesign
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified: [C:/Users/bskim/Downloads/beamacademy-index.html]
autonomous: false

must_haves:
  truths:
    - "All Access hero banner is refined to complement (not overshadow) pricing tiers"
    - "Section header and divider have consistent v2 typography"
    - "Pricing note at bottom is styled subtly"
    - "Human verification confirms entire section looks cohesive on desktop and mobile"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Final polish for courses section"
      contains: "all-access-hero"
  key_links:
    - from: ".all-access-hero"
      to: "onclick handler"
      via: "openAllAccessModal"
      pattern: "openAllAccessModal"
---

<objective>
Apply final polish to courses section and verify cohesive design

Purpose: After Plans 01-02 update pricing tiers and course browser, this plan ensures the All Access banner, section headers, and subtle elements form a cohesive whole. Ends with human verification checkpoint.

Output: Polished, cohesive courses section matching v2 design quality across all components
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-courses-section-redesign/14-CONTEXT.md
@.planning/phases/14-courses-section-redesign/14-RESEARCH.md
@.planning/phases/14-courses-section-redesign/14-01-SUMMARY.md
@.planning/phases/14-courses-section-redesign/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine All Access hero banner styling</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
The All Access banner is already prominent. Refine to complement (not compete with) pricing tiers:

1. **Banner container (.all-access-hero):**
   - Keep gradient background but reduce intensity slightly
   - Border-radius: `24px` (match pricing cards)
   - Ensure hover effect is subtle (slight brightness increase)
   - Padding: adjust if needed for balance

2. **Badge (.all-access-hero-badge):**
   - Match v2 badge style: smaller, pill-shaped
   - Background: `var(--purple-600)` or gradient
   - Font-size: `0.75rem`
   - Padding: `6px 14px`
   - Border-radius: `100px`

3. **Title (.all-access-hero-title):**
   - Font-size: clamp(1.5rem, 4vw, 2rem) - responsive but not overwhelming
   - Font-weight: 700

4. **Description (.all-access-hero-desc):**
   - Color: `rgba(255, 255, 255, 0.8)`
   - Max-width: `400px`

5. **Price boxes (.all-access-price-box):**
   - Background: `rgba(255, 255, 255, 0.1)` (subtle contrast)
   - Border-radius: `12px`
   - Padding: `16px 24px`

6. **CTA (.all-access-hero-cta):**
   - Match button style from course cards
   - Background: white, color purple
   - Border-radius: `100px`

PRESERVE: `onclick="openAllAccessModal()"` handler on banner.
  </action>
  <verify>
Test in browser:
- Banner looks polished but doesn't visually overpower pricing tiers below
- All interactive elements (hover, click) still work
- Mobile layout maintains readability
  </verify>
  <done>All Access banner refined to complement pricing tiers with v2 styling</done>
</task>

<task type="auto">
  <name>Task 2: Polish section headers and supporting text</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Ensure consistent typography and styling for supporting elements:

1. **Section tag (.section-tag in courses-pricing):**
   - Already exists, verify matches v2 pattern:
   - Uppercase, 0.85rem, purple-300 (on dark bg), letter-spacing 0.1em

2. **Section title:**
   - Verify using clamp typography for responsive sizing
   - If not, add: font-size: clamp(2rem, 5vw, 2.75rem)

3. **Section description:**
   - Color: `var(--gray-400)`
   - Max-width: `600px`, centered
   - Font-size: `1.1rem`

4. **Course browser divider (.course-browser-divider):**
   - Add subtle top border or padding to separate from pricing
   - Margin-top: `48px` or `64px`
   - Padding-top: `32px`

5. **Course browser heading (.course-browser-heading):**
   - Font-size: clamp(1.5rem, 4vw, 2rem)
   - Font-weight: 700
   - Color: var(--white)
   - Text-align: center

6. **Course browser subtext (.course-browser-subtext):**
   - Font-size: `1rem`
   - Color: `var(--gray-400)`
   - Text-align: center
   - Margin-bottom: `24px`

7. **Pricing note (.pricing-note):**
   - Font-size: `0.9rem`
   - Color: `var(--gray-500)`
   - Text-align: center
   - Margin-top: `32px`
   - Subtle styling - informational, not prominent

8. **Enrollment info prompt (.enrollment-info-prompt):**
   - Background: `rgba(124, 58, 237, 0.1)`
   - Border-left: `3px solid var(--purple-500)` or border-radius with subtle border
   - Padding: `16px 20px`
   - Font-size: `0.95rem`
   - Margin-bottom: `24px`
  </action>
  <verify>
Test in browser:
- Typography hierarchy is clear (section title > subsection heading > body text)
- Supporting text is readable but not competing with main content
- Visual flow from pricing to course browser is smooth
  </verify>
  <done>Section headers and supporting text have consistent v2 typography</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete courses section redesign:
- Pricing tier cards with v2 styling and featured tier highlighting
- Course browser with v2 tabs and scannable course cards
- Refined All Access banner
- Consistent typography throughout
  </what-built>
  <how-to-verify>
1. Open C:/Users/bskim/Downloads/beamacademy-index.html in browser
2. Navigate to "Invest in Your Future" section (click Courses in nav)

**Desktop verification (1200px+ viewport):**
- [ ] Pricing tiers display as 3 columns
- [ ] Intermediate tier is visually highlighted (badge, purple accent)
- [ ] Hover on pricing tier shows elevation effect
- [ ] All Access banner is prominent but not overwhelming
- [ ] Course browser tabs are pill-styled
- [ ] Course cards have consistent styling with truncated descriptions
- [ ] Grid shows 3 course cards per row

**Mobile verification (375px viewport or Chrome DevTools mobile):**
- [ ] Pricing tiers stack vertically
- [ ] Featured tier appears at top
- [ ] All Access banner is readable
- [ ] Year tabs wrap nicely if needed
- [ ] Course cards stack single column
- [ ] No horizontal overflow

**Functional verification:**
- [ ] Click a package in pricing tier - does form update? (if feature exists)
- [ ] Click year tabs - do course cards filter correctly?
- [ ] Click "Try Free" on course card - scrolls to contact form?
- [ ] Click All Access banner - modal opens? (if feature exists)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks good, or describe issues for revision</resume-signal>
</task>

</tasks>

<verification>
After human approval:
1. Pricing section matches Hero/Trust visual quality
2. Course browser is modernized with improved UX
3. All interactive functionality preserved
4. Mobile responsive on all components
5. Consistent v2 design language throughout
</verification>

<success_criteria>
- All Access banner refined to complement pricing tiers
- Section typography consistent with v2 patterns
- Human verification passed on desktop and mobile
- All interactive features working (tabs, package selection, links)
- Phase 14 success criteria met: Pricing section matches Hero/Trust quality
</success_criteria>

<output>
After completion, create `.planning/phases/14-courses-section-redesign/14-03-SUMMARY.md`
</output>
