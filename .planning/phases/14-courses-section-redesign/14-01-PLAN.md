---
phase: 14-courses-section-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [C:/Users/bskim/Downloads/beamacademy-index.html]
autonomous: true

must_haves:
  truths:
    - "Pricing tier cards have v2 visual styling (rounded corners, subtle borders, hover effects)"
    - "Intermediate tier is visually highlighted as featured/recommended"
    - "Price display is prominent with clear hierarchy (amount > period > from)"
    - "Package list items have clear hover states and selection feedback"
    - "Mobile stacking works correctly with featured tier prominent"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Pricing tier v2 CSS and HTML updates"
      contains: "pricing-tier-new"
  key_links:
    - from: ".pricing-tier-new.featured-tier"
      to: "visual prominence"
      via: "CSS border, shadow, or badge"
      pattern: "featured-tier"
    - from: ".tier-packages li"
      to: "onclick handlers"
      via: "preserved data attributes"
      pattern: "selectPackage"
---

<objective>
Modernize pricing tier cards to match v2 design language (Hero/Trust sections)

Purpose: Pricing tiers are functional but visually dated compared to Hero/Trust sections. This plan applies v2 card styling patterns - rounded corners, subtle borders, hover effects, and proper featured tier highlighting.

Output: Visually modern 3-column pricing layout with clear hierarchy and featured tier emphasis
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-courses-section-redesign/14-CONTEXT.md
@.planning/phases/14-courses-section-redesign/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pricing tier card CSS to v2 styling</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update the `.pricing-tier-new` CSS rules to match v2 card patterns:

1. **Card base styling:**
   - Background: `rgba(255, 255, 255, 0.03)` (subtle on dark bg)
   - Border: `1px solid rgba(255, 255, 255, 0.1)`
   - Border-radius: `24px` (v2 standard)
   - Padding: `32px`
   - Transition: `all 0.4s cubic-bezier(0.4, 0, 0.2, 1)`

2. **Hover state:**
   - Transform: `translateY(-4px)`
   - Border-color: `rgba(124, 58, 237, 0.4)` (purple accent)
   - Box-shadow: `0 20px 60px rgba(124, 58, 237, 0.15)`

3. **Featured tier styling (add `.featured-tier` class to Intermediate article):**
   - Background: `rgba(124, 58, 237, 0.1)` (purple tint)
   - Border: `2px solid var(--purple-500)`
   - Add "Most Popular" badge using `::before` pseudo-element positioned at top center:
     - Position: absolute, top: -12px, left: 50%, transform: translateX(-50%)
     - Background: `linear-gradient(135deg, var(--purple-600), var(--purple-700))`
     - Color: white, padding: 8px 20px, border-radius: 100px
     - Font: 0.8rem, weight 600, uppercase, letter-spacing 0.05em

4. **Price styling improvements:**
   - `.price-amount`: font-size 3rem, font-weight 700, line-height 1
   - `.price-from`, `.price-period`: font-size 0.9rem, color var(--gray-400)

PRESERVE: All `onclick` handlers, `data-tier`, `data-pkg` attributes on package list items.
  </action>
  <verify>
Inspect the pricing tiers in browser:
- Cards have rounded corners and subtle borders
- Hover causes upward movement and purple glow
- Intermediate tier has purple tint and "Most Popular" badge
  </verify>
  <done>Pricing tier cards match v2 visual style with featured tier clearly highlighted</done>
</task>

<task type="auto">
  <name>Task 2: Update package list styling within tiers</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update `.tier-packages` styling for cleaner list presentation:

1. **List container:**
   - Remove default list styling (list-style: none, padding: 0)
   - Add vertical gap between items: margin-bottom on li items or gap if using flex

2. **Package item styling:**
   - Background: `rgba(255, 255, 255, 0.03)`
   - Border: `1px solid rgba(255, 255, 255, 0.08)`
   - Border-radius: `12px`
   - Padding: `12px 16px`
   - Display: flex, justify-content: space-between, align-items: center
   - Cursor: pointer
   - Transition: all 0.2s ease

3. **Package item hover:**
   - Background: `rgba(255, 255, 255, 0.06)`
   - Border-color: `var(--purple-400)`
   - Transform: `translateX(4px)` (subtle right shift)

4. **Package item selected state (when .selected class applied):**
   - Background: `rgba(124, 58, 237, 0.15)`
   - Border-color: `var(--purple-500)`

5. **Package badges (.pkg-badge, .pkg-save):**
   - `.pkg-badge`: background purple-600, color white, font-size 0.7rem, padding 4px 8px, border-radius 100px
   - `.pkg-save`: color var(--emerald-400) or var(--purple-300), font-size 0.8rem

6. **All Access item styling (li.all-access):**
   - Subtle gradient background: `linear-gradient(135deg, rgba(124, 58, 237, 0.1), rgba(124, 58, 237, 0.05))`
   - Slightly more prominent border

PRESERVE: All `onclick` handlers, `data-tier`, `data-pkg` attributes.
  </action>
  <verify>
Test in browser:
- Package items are clearly clickable with hover feedback
- Badges are visible and styled as pills
- All Access items have premium appearance
- Click a package and verify onclick still fires (check console or form update)
  </verify>
  <done>Package list items have modern styling with clear interaction states</done>
</task>

<task type="auto">
  <name>Task 3: Mobile responsive adjustments for pricing tiers</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update mobile media queries (max-width: 768px) for pricing tiers:

1. **Grid stacking:**
   - `.pricing-tiers-new`: grid-template-columns: 1fr (single column)
   - Gap: 20px

2. **Featured tier on mobile:**
   - Remove scale transforms (if any)
   - Add `order: -1` to move featured tier to top of stack
   - Keep border and background highlight but reduce shadow intensity

3. **Card padding on mobile:**
   - Reduce padding to 24px
   - Reduce price font-size slightly (2.5rem instead of 3rem)

4. **Package list on mobile:**
   - Keep full-width layout
   - Ensure touch targets are minimum 44px height
   - Maintain gap between items

Test on mobile viewport (375px width).
  </action>
  <verify>
Test at 375px viewport width:
- Tiers stack vertically
- Featured tier appears first (at top)
- Touch targets are comfortable size
- No horizontal overflow
  </verify>
  <done>Pricing tiers responsive on mobile with featured tier prominent at top</done>
</task>

</tasks>

<verification>
After all tasks:
1. Desktop: 3-column layout, featured tier visually prominent, hover effects work
2. Mobile: Single column, featured tier at top, no overflow
3. Functionality: Package selection still works (onclick handlers preserved)
4. Visual: Matches v2 design language (Hero/Trust sections)
</verification>

<success_criteria>
- Pricing tier cards have v2 styling (24px radius, subtle borders, hover elevation)
- Intermediate tier is clearly featured with badge and purple accent
- Package list items have clear hover/selected states
- Mobile stacks correctly with featured tier first
- All onclick handlers and data attributes preserved
</success_criteria>

<output>
After completion, create `.planning/phases/14-courses-section-redesign/14-01-SUMMARY.md`
</output>
