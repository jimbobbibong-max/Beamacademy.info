---
phase: 14-courses-section-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [C:/Users/bskim/Downloads/beamacademy-index.html]
autonomous: true

must_haves:
  truths:
    - "Course cards have v2 styling matching pricing tier cards"
    - "Year tabs have v2 pill styling with clear active state"
    - "Course cards are scannable (icon > title > year badge visible at glance)"
    - "Grid adapts smoothly across viewport widths (no orphan cards)"
    - "Tab switching still filters courses correctly"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Course browser v2 CSS updates"
      contains: "course-card"
  key_links:
    - from: ".year-tab"
      to: "data-year filtering"
      via: "JavaScript tab handler"
      pattern: "year-tab.*active"
    - from: ".course-card"
      to: "data-years attribute"
      via: "show/hide based on tab selection"
      pattern: "data-years"
---

<objective>
Modernize course browser to match v2 design language with improved scannability

Purpose: Course browser has 24 cards that need better visual hierarchy and scannability. This plan updates year tabs to v2 pill styling and course cards to match the modernized pricing tier aesthetic.

Output: Visually modern tabbed course browser with scannable cards and smooth responsive grid
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-courses-section-redesign/14-CONTEXT.md
@.planning/phases/14-courses-section-redesign/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update year tabs to v2 pill styling</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update `.year-tabs` and `.year-tab` CSS for v2 styling:

1. **Tab container (.year-tabs):**
   - Display: flex
   - Justify-content: center
   - Gap: 8px
   - Margin-bottom: 32px
   - Flex-wrap: wrap (for mobile)

2. **Tab button styling (.year-tab):**
   - Background: transparent
   - Border: `1px solid rgba(255, 255, 255, 0.2)`
   - Color: `var(--gray-400)`
   - Padding: `12px 24px`
   - Border-radius: `100px` (full pill)
   - Font-family: `var(--font-display)` if available, else inherit
   - Font-size: `0.95rem`
   - Font-weight: `500`
   - Cursor: pointer
   - Transition: `all 0.2s ease`

3. **Tab hover state (.year-tab:hover):**
   - Border-color: `var(--purple-400)`
   - Color: `var(--white)`

4. **Tab active state (.year-tab.active):**
   - Background: `var(--purple-600)`
   - Border-color: `var(--purple-600)`
   - Color: `white`

PRESERVE: All `role`, `aria-selected`, and `data-year` attributes on tabs.
  </action>
  <verify>
Test in browser:
- Tabs display as pills with centered layout
- Inactive tabs have subtle border, active tab has purple fill
- Hover shows purple border highlight
- Click tab and verify filtering still works
  </verify>
  <done>Year tabs have modern v2 pill styling with clear active/hover states</done>
</task>

<task type="auto">
  <name>Task 2: Update course card styling for scannability</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update `.course-card` CSS for v2 styling and improved scannability:

1. **Card base styling:**
   - Background: `rgba(255, 255, 255, 0.02)`
   - Border: `1px solid rgba(255, 255, 255, 0.08)`
   - Border-radius: `16px`
   - Padding: `24px`
   - Display: flex
   - Flex-direction: column
   - Gap: `12px`
   - Transition: `all 0.3s ease`

2. **Card hover state:**
   - Background: `rgba(255, 255, 255, 0.05)`
   - Border-color: `rgba(124, 58, 237, 0.3)`
   - Transform: `translateY(-2px)`

3. **Course icon (.course-icon):**
   - Font-size: `2rem`
   - Margin-bottom: `8px`

4. **Course title (h3):**
   - Font-size: `1.1rem`
   - Font-weight: `600`
   - Color: `var(--white)`
   - Margin: 0

5. **Course description (p):**
   - Font-size: `0.9rem`
   - Color: `var(--gray-400)`
   - Line-height: `1.5`
   - Display: `-webkit-box`
   - `-webkit-line-clamp: 2` (truncate to 2 lines)
   - `-webkit-box-orient: vertical`
   - Overflow: hidden

6. **Course card header (.course-card-header):**
   - Display: flex
   - Justify-content: space-between
   - Align-items: center
   - Margin-bottom: `8px`

7. **Year badge (.course-year-badge):**
   - Background: `var(--purple-600)`
   - Color: white
   - Padding: `4px 12px`
   - Border-radius: `100px`
   - Font-size: `0.75rem`
   - Font-weight: `600`

8. **Course footer (.course-card-footer):**
   - Display: flex
   - Justify-content: space-between
   - Align-items: center
   - Margin-top: auto (push to bottom)

9. **Course price (.course-price):**
   - Font-size: `1.15rem`
   - Font-weight: `700`
   - Color: `var(--white)`
   - The /week span: font-size 0.8rem, font-weight 400, color var(--gray-400)

10. **Course CTA button (.course-btn):**
    - Background: transparent
    - Border: `1px solid var(--purple-500)`
    - Color: `var(--purple-400)`
    - Padding: `8px 16px`
    - Border-radius: `100px`
    - Font-size: `0.85rem`
    - Font-weight: `600`
    - Transition: `all 0.2s ease`
    - Hover: background var(--purple-600), border-color var(--purple-600), color white

PRESERVE: All `data-years` attributes on course cards, all `href="#contact"` links.
  </action>
  <verify>
Test in browser:
- Cards have subtle background with hover elevation
- Description truncates to 2 lines
- Price and CTA button are clearly visible at bottom
- Year badges are colored pills
  </verify>
  <done>Course cards have v2 styling with improved visual hierarchy and scannability</done>
</task>

<task type="auto">
  <name>Task 3: Update courses grid for smooth responsive behavior</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update `.courses-grid` for fluid responsive behavior:

1. **Grid container:**
   - Display: grid
   - Grid-template-columns: `repeat(auto-fit, minmax(280px, 1fr))`
   - Gap: `20px`

This ensures:
- Cards automatically flow to fit viewport width
- Minimum card width 280px prevents cards from getting too narrow
- Maximum 1fr prevents cards from getting too wide
- No orphan cards at awkward widths

2. **Mobile adjustments (max-width: 640px):**
   - Grid-template-columns: `1fr` (single column)
   - Gap: `16px`

3. **Tablet adjustments (641px - 1024px):**
   - Grid should naturally show 2 columns via auto-fit

4. **Desktop (1024px+):**
   - Grid should naturally show 3 columns via auto-fit

No JavaScript changes needed - CSS handles the responsive behavior.
  </action>
  <verify>
Test at multiple viewport widths:
- 375px: single column
- 768px: 2 columns
- 1200px: 3 columns
- No cards appearing isolated on last row at awkward widths
  </verify>
  <done>Courses grid adapts smoothly across all viewport widths</done>
</task>

</tasks>

<verification>
After all tasks:
1. Year tabs: v2 pill styling, active state clear, filtering works
2. Course cards: Scannable hierarchy, hover effects, description truncation
3. Grid: Smooth responsive behavior at all widths
4. Functionality: Tab switching and course card links preserved
</verification>

<success_criteria>
- Year tabs have v2 pill styling with purple active state
- Course cards match v2 aesthetic (16px radius, subtle borders, hover elevation)
- Description text truncates to 2 lines for scannability
- Grid uses auto-fit for smooth responsive behavior
- All data attributes and links preserved
</success_criteria>

<output>
After completion, create `.planning/phases/14-courses-section-redesign/14-02-SUMMARY.md`
</output>
