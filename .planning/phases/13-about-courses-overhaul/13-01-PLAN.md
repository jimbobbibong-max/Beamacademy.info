---
phase: 13-about-courses-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/bskim/Downloads/beamacademy-index.html
autonomous: false

must_haves:
  truths:
    - "About section has 'About Us' section tag above title"
    - "About section title uses v2 clamp typography"
    - "About badge uses v2 pill style with border and softer colors"
    - "Both sections render correctly on mobile (375px-1024px)"
  artifacts:
    - path: "C:/Users/bskim/Downloads/beamacademy-index.html"
      provides: "Updated About section with v2 patterns"
      contains: "about-section-tag"
  key_links:
    - from: "About section HTML"
      to: "section-tag CSS"
      via: "class about-section-tag"
      pattern: "about-section-tag"
    - from: "About h2"
      to: "v2 typography"
      via: "section-title class or updated CSS"
      pattern: "clamp.*2rem.*2\\.75rem"
---

<objective>
Modernize About section to match v2 design quality established in Phases 6-11.

Purpose: Complete v3 milestone by aligning remaining legacy sections with v2 visual patterns
Output: About section with section-tag, v2 typography, and modernized badge styling
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-about-courses-overhaul/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add section-tag and update About typography</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
1. **Add section-tag to About section** (around line 5731-5732):
   - Inside `.about-text`, add `<div class="about-section-tag">About Us</div>` BEFORE the h2

2. **Add CSS for about-section-tag** (after line 1858, before `.about-text h2`):
   ```css
   .about-section-tag {
       display: inline-block;
       font-family: var(--font-display);
       font-size: 0.85rem;
       font-weight: 600;
       color: var(--purple-600);
       text-transform: uppercase;
       letter-spacing: 0.1em;
       margin-bottom: 16px;
   }
   ```

3. **Update About h2 typography** (line 1859-1863):
   - Change `.about-text h2` font-size from `2.25rem` to `clamp(2rem, 4vw, 2.75rem)` to match `.section-title`

Why NOT use `.section-title` class directly: The About section is a 2-column layout with left-aligned text. Adding the standard `.section-title` class would work but keeping the scoped `.about-text h2` selector is cleaner since it's already scoped.
  </action>
  <verify>
- Search for "about-section-tag" in HTML - should appear once in About section
- Search for ".about-section-tag" in CSS - should have display, font, color properties
- Search for "clamp" near ".about-text h2" - should show the updated font-size
  </verify>
  <done>
- "About Us" tag appears above "Building Education And Mastery" heading
- Heading typography uses responsive clamp sizing
  </done>
</task>

<task type="auto">
  <name>Task 2: Update About badge to v2 pill style</name>
  <files>C:/Users/bskim/Downloads/beamacademy-index.html</files>
  <action>
Update `.about-badge` CSS (lines 1839-1849) to use v2 softer pill style while keeping absolute positioning:

**Before:**
```css
.about-badge {
    position: absolute;
    bottom: -20px;
    right: -20px;
    background: var(--purple-700);
    color: white;
    padding: 20px 28px;
    border-radius: 16px;
    text-align: center;
    box-shadow: 0 12px 40px rgba(124, 58, 237, 0.3);
}
```

**After:**
```css
.about-badge {
    position: absolute;
    bottom: -20px;
    right: -20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    background: var(--purple-100);
    color: var(--purple-700);
    padding: 16px 24px;
    border-radius: 16px;
    border: 2px solid var(--purple-200);
    text-align: center;
    box-shadow: 0 8px 24px rgba(124, 58, 237, 0.15);
}
```

Key changes:
- Background: `--purple-700` (dark) -> `--purple-100` (light)
- Text color: white -> `--purple-700`
- Added: `border: 2px solid var(--purple-200)`
- Softer shadow: reduced from 40px blur to 24px
- Added flex column for better alignment

DO NOT change `.about-badge-number` or `.about-badge-text` - they inherit from parent and will work with new colors.
  </action>
  <verify>
- Search for ".about-badge {" and verify:
  - `background: var(--purple-100)`
  - `color: var(--purple-700)`
  - `border: 2px solid var(--purple-200)`
  </verify>
  <done>
- Badge has softer purple background instead of dark purple
- Badge text is purple-700 on light background
- Badge has subtle border matching v2 pattern
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
About section modernized with v2 patterns:
1. "About Us" section-tag above the title
2. Responsive clamp typography on heading
3. Badge with softer v2 pill styling (light purple background, border)
  </what-built>
  <how-to-verify>
1. Open C:/Users/bskim/Downloads/beamacademy-index.html in browser
2. Scroll to About section ("Building Education And Mastery")
3. Verify:
   - "ABOUT US" appears above the main heading in purple uppercase text
   - Heading "Building Education And Mastery" has proper sizing
   - BHHS Graduates badge on image has light purple background (not dark purple)
   - Badge has visible border and looks softer/more modern

4. **Mobile check** - Open DevTools (F12), toggle device toolbar, test at:
   - 768px: Two-column layout should still work
   - 600px: Check feature grid stacks properly
   - 375px: Entire About section stacks, badge still visible

5. **Courses section quick check** - Scroll down to Courses, verify:
   - Still has "COURSES & PRICING" tag
   - Dark theme maintained
   - No visual regressions
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After human verification:
- Grep for "about-section-tag" confirms HTML element exists
- Grep for "clamp.*2rem.*2.75rem" near about shows v2 typography
- Grep for "purple-100" in about-badge confirms pill style
- Visual inspection confirms both sections render correctly
</verification>

<success_criteria>
1. About section has "About Us" section-tag above heading
2. About heading uses clamp typography like other v2 sections
3. About badge uses soft pill style (light bg, dark text, border)
4. Courses section unchanged and still functional
5. Mobile responsive at 375px, 600px, 768px, 1024px
</success_criteria>

<output>
After completion, create `.planning/phases/13-about-courses-overhaul/13-01-SUMMARY.md`
</output>
